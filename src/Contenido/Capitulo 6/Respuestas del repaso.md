**1. E.** Las opciones A y B no compilarán porque los constructores no pueden ser llamados sin `new`. Las opciones C y D compilarán pero crearán un nuevo objeto en lugar de configurar los campos en este. El resultado es que el programa imprimirá 0, no 2, en tiempo de ejecución. Llamar a un constructor sobrecargado, usando `this()`, o a un constructor padre, usando `super()`, solo está permitido en la primera línea del constructor, lo que hace que la opción E sea correcta y la opción F incorrecta. Finalmente, la opción G es incorrecta porque el programa imprime 0 sin ningún cambio, no 2.

**2. A, B, F.** El modificador `final` se puede usar con `private` y `static`, haciendo que las opciones A y F sean correctas. Marcar un método `private` como `final` es redundante pero permitido. Un método `private` también puede marcarse como `static`, haciendo que la opción B sea correcta. Las opciones C, D y E son incorrectas porque los métodos marcados como `static`, `private` o `final` no pueden ser sobrescritos (_overridden_); por lo tanto, no pueden ser marcados como `abstract`.

**3. B, C.** Los métodos sobrecargados (_overloaded_) tienen el mismo nombre de método pero una firma diferente (los parámetros del método difieren), lo que hace que la opción A sea incorrecta. Los métodos de instancia sobrescritos (_overridden_) y los métodos `static` ocultos (_hidden_) deben tener la misma firma (el nombre y los parámetros del método deben coincidir), haciendo que las opciones B y C sean correctas. Los métodos sobrecargados pueden tener diferentes tipos de retorno, mientras que los métodos sobrescritos y ocultos pueden tener tipos de retorno covariantes. No se requiere que ninguno de estos métodos use el mismo tipo de retorno, haciendo que las opciones D, E y F sean incorrectas.

**4. F.** El código no compilará tal como está, porque la clase padre `Mammal` no define un constructor sin argumentos (_no-argument constructor_). Por esta razón, la primera línea de un constructor `Platypus` debería ser una llamada explícita a `super(int)`, haciendo que la opción F sea la respuesta correcta. La opción E es incorrecta, ya que la línea 7 compila sin problemas. El método `sneeze()` en la clase `Mammal` está marcado como `private`, lo que significa que no se hereda y, por lo tanto, no se sobrescribe en la clase `Platypus`. Por esta razón, el método `sneeze()` en la clase `Platypus` es libre de definir el mismo método con cualquier tipo de retorno.

**5. E.** El código compila, haciendo que la opción F sea incorrecta. Una variable de instancia con el mismo nombre que una variable de instancia heredada se oculta, no se sobrescribe. Esto significa que ambas variables existen, y la que se usa depende de la ubicación y el tipo de referencia. Debido a que el método `main()` usa un tipo de referencia de `Speedster` para acceder a la variable `numSpots`, la variable en la clase `Speedster`, no en la clase `Cheetah`, debe configurarse en 50. La opción A es incorrecta, ya que reasigna el parámetro del método a sí mismo. La opción B es incorrecta, ya que le asigna al parámetro del método el valor de la variable de instancia en `Cheetah`, el cual es 0. La opción C es incorrecta, ya que le asigna el valor a la variable de instancia en `Cheetah`, no en `Speedster`. La opción D es incorrecta, ya que le asigna al parámetro del método el valor de la variable de instancia en `Speedster`, el cual es 0. Las opciones A, B, C y D imprimen 0 en tiempo de ejecución. La opción E es la respuesta correcta, ya que asigna a la variable de instancia `numSpots` en la clase `Speedster` un valor de 50. La variable `numSpots` en la clase `Speedster` se referencia luego correctamente en el método `main()`, imprimiendo 50 en tiempo de ejecución.

**6. D, E.** La clase `Moose` no compila, ya que la variable `final` `antlers` no se inicializa cuando se declara, en un inicializador de instancia o en un constructor. `Caribou` y `Reindeer` no son inmutables porque no están marcadas como `final`, lo que significa que una subclase podría extenderlas y agregar campos mutables. `Elk` y `Deer` son ambas clases inmutables ya que están marcadas como `final` y solo incluyen miembros `private final`, haciendo que las opciones D y E sean correctas. Como se muestra con `Elk`, una clase no necesita declarar ningún campo para ser considerada inmutable.

**7. A.** El código compila y se ejecuta sin problema, por lo que las opciones E y F son incorrectas. La clase `Arthropod` define dos versiones sobrecargadas (_overloaded_) del método `printName()`. El método `printName()` que toma un valor `int` en la línea 5 es sobrescrito (_overridden_) correctamente en la clase `Spider` en la línea 9. **Se debe recordar**, un método sobrescrito puede tener un modificador de acceso más amplio, y el acceso `protected` es más amplio que el acceso de paquete (_package-private_). Debido al polimorfismo, el método sobrescrito reemplaza al método en todas las llamadas, incluso si se usa una variable de referencia `Arthropod`, como se hace en el método `main()`. Por estas razones, se llama al método sobrescrito en las líneas 14 y 15, imprimiendo `Spider` dos veces. Note que el valor `short` se moldea (_cast_) automáticamente al tipo más grande de `int`, que luego usa el método sobrescrito. La línea 16 llama al método sobrecargado en la clase `Arthropod`, ya que el valor `long` `5L` no coincide con el método sobrescrito, lo que resulta en la impresión de `Arthropod`. Por lo tanto, la opción A es la respuesta correcta.

**8. D.** El código compila sin problema. La pregunta se asegura de que **se sepa** que los constructores de la superclase se llaman de la misma manera en las clases abstractas que en las clases no abstractas. La línea 9 llama al constructor en la línea 6. El compilador inserta automáticamente `super()` como la primera línea del constructor definido en la línea 6. El programa luego llama al constructor en la línea 3 e imprime `Wow-`. El control luego regresa a la línea 6, y se imprime `Oh-`. Finalmente, la llamada al método en la línea 10 usa la versión de `fly()` en la clase `Pelican`, puesto que está marcada como `private` y el tipo de referencia de `var` se resuelve como `Pelican`. La salida final es `Wow-Oh-Pelican`, haciendo que la opción D sea la respuesta correcta. **Se debe recordar** que los métodos `private` no pueden ser sobrescritos. Si el tipo de referencia de `chirp` fuera `Bird`, entonces el código no compilaría, ya que no sería accesible fuera de la clase.

**9. B, E.** La firma debe coincidir exactamente, haciendo que la opción A sea incorrecta. No existe tal cosa como una firma covariante. Un método sobrescrito no debe declarar ninguna excepción verificada (_checked exception_) nueva o una excepción verificada que sea más amplia que el método heredado. Por esta razón, la opción B es correcta y la opción D es incorrecta. La opción C es incorrecta porque un método sobrescrito puede tener el mismo modificador de acceso que la versión en la clase padre. Finalmente, los métodos sobrescritos deben tener tipos de retorno covariantes, y solo `void` es covariante con `void`, haciendo que la opción E sea correcta.

**10. A, C.** La opción A es correcta, ya que `this(3)` llama al constructor declarado en la línea 5, mientras que `this("")` llama al constructor declarado en la línea 10. La opción B no compila, ya que insertar `this()` en la línea 3 resulta en un error del compilador, puesto que no hay un constructor coincidente. La opción C es correcta, ya que `short` puede ser moldeado (_cast_) implícitamente a `int`, lo que resulta en que `this((short)1)` llame al constructor declarado en la línea 5. Además, `this(null)` llama al constructor `String` declarado en la línea 10. La opción D no compila porque insertar `super()` en la línea 14 resulta en una llamada a constructor inválida. La clase `Howler` no contiene un constructor sin argumentos. La opción E también es incorrecta. Insertar `this(2L)` en la línea 3 resulta en una definición de constructor recursiva. El compilador detecta esto y reporta un error. La opción F es incorrecta, ya que usar `super(null)` en la línea 14 no coincide con ningún constructor padre. Si se usara un _cast_ explícito, como `super((Integer)null)`, entonces el código habría compilado pero lanzaría una excepción en tiempo de ejecución durante el _unboxing_. Finalmente, la opción G es incorrecta porque la superclase `Howler` no contiene un constructor sin argumentos. Por lo tanto, el constructor declarado en la línea 13 no compilará sin una llamada explícita a un constructor sobrecargado o padre.

**11. C.** El código compila y se ejecuta sin problema, haciendo que las opciones F y G sean incorrectas. La línea 16 inicializa una instancia de `PolarBear` y la asigna a la referencia `bear`. La declaración de la variable y los inicializadores de instancia se ejecutan primero, configurando `value` a `tac`. Se llama al constructor declarado en la línea 5, resultando en que `value` se configure a `tacb`. **Se debe recordar**, un método `main()` `static` puede acceder a constructores `private` declarados en la misma clase. La línea 17 crea otra instancia de `PolarBear`, reemplazando la referencia `bear` declarada en la línea 16. Primero, `value` se inicializa a `tac` como antes. La línea 17 llama al constructor declarado en la línea 8, puesto que `String` es la coincidencia más estrecha de un literal `String`. Este constructor luego llama al constructor sobrecargado declarado en la línea 5, resultando en que `value` se actualice a `tacb`. El control regresa al constructor anterior, con la línea 10 actualizando `value` a `tacbf`, haciendo de la opción C la respuesta correcta. Note que si el constructor declarado en la línea 8 no existiera, entonces el constructor en la línea 12 coincidiría. Finalmente, la referencia `bear` se moldea (_cast_) adecuadamente a `PolarBear` en la línea 18, haciendo accesible el parámetro `value`.

**12. C.** El código no compila, por lo que la opción A es incorrecta. El primer error de compilación está en la línea 8. Puesto que `Rodent` declara al menos un constructor, y no es un constructor sin argumentos, `Beaver` debe declarar un constructor con una llamada explícita a un constructor `super()`. La línea 9 contiene dos errores de compilación. Primero, los tipos de retorno no son covariantes ya que `Number` es un supertipo, no un subtipo, de `Integer`. Segundo, el método heredado es `static`, pero el método sobrescrito no lo es, haciendo de esto una sobrescritura inválida. El código contiene tres errores de compilación, aunque están limitados a dos líneas, haciendo de la opción C la respuesta correcta.

**13. A, G.** El compilador insertará un constructor por defecto sin argumentos si la clase compila y no define ningún constructor. Las opciones A y G cumplen este requisito, haciéndolas las respuestas correctas. La declaración `bird()` en la opción G es una declaración de método, no un constructor. Las opciones B y C no compilan. Puesto que el nombre del constructor no coincide con el nombre de la clase, el compilador los trata como métodos a los que les falta un tipo de retorno. Las opciones D, E y F compilan todas, pero puesto que declaran al menos un constructor, el compilador no suministra uno.

**14. B, E, F.** Una clase solo puede extender directamente a una única clase, lo que hace que la opción A sea incorrecta. Sin embargo, una clase puede implementar cualquier número de interfaces, lo que hace que la opción B sea correcta. La opción C es incorrecta porque los tipos de variables primitivas no heredan de `java.lang.Object`. Si una clase extiende a otra clase, entonces es una subclase, no una superclase, haciendo que la opción D sea incorrecta. Una clase que implementa una interfaz es un subtipo de esa interfaz, haciendo que la opción E sea correcta. Finalmente, la opción F es correcta ya que es una descripción precisa de la herencia múltiple, la cual no está permitida en Java.

**15. C.** El código no compila porque el método `isBlind()` en `Nocturnal` no está marcado como `abstract` y no contiene un cuerpo de método. El resto de las líneas compilan sin problema, haciendo de la opción C la respuesta correcta. Si se añadiera el modificador `abstract` a la línea 2, entonces el código compilaría e imprimiría `false` en tiempo de ejecución, haciendo que la opción B fuera la respuesta correcta.

**16. D.** El código compila, por lo que la opción G es incorrecta. Basado en el orden de inicialización, los componentes `static` se inicializan primero, comenzando con la clase `Arachnid`, puesto que es el padre de la clase `Scorpion`, lo cual inicializa el `StringBuilder` a `u`. El inicializador `static` en `Scorpion` luego actualiza `sb` para contener `uq`, lo cual es impreso dos veces por las líneas 13 y 14 junto con espacios separando los valores. A continuación, se inicializa una instancia de `Arachnid` en la línea 15. Hay dos inicializadores de instancia en `Arachnid`, y se ejecutan en orden, anexando `cr` al `StringBuilder`, resultando en un valor de `uqcr`. Luego se inicializa una instancia de `Scorpion` en la línea 16. Los inicializadores de instancia en la superclase `Arachnid` se ejecutan primero, anexando `cr` de nuevo y actualizando el valor de `sb` a `uqcrcr`. Finalmente, el inicializador de instancia en `Scorpion` se ejecuta y anexa `m`. El programa se completa con el valor final impreso siendo `uq uq uqcrcrm`, haciendo que la opción D sea la respuesta correcta.

**17. C, F.** Llamar a un constructor sobrecargado con `this()` solo se puede utilizar como la primera línea de un constructor, haciendo que las opciones A y B sean incorrectas. Acceder a `this.variableName` puede realizarse desde cualquier método de instancia, constructor o inicializador de instancia, pero no desde un método `static` o un inicializador `static`. Por esta razón, la opción C es correcta y la opción D es incorrecta. La opción E es engañosa. El constructor por defecto es escrito por el compilador solo si no se proporcionaron constructores definidos por el usuario. Y `this()` solo puede ser llamado desde un constructor en la misma clase. Puesto que no puede haber constructores definidos por el usuario en la clase si se creó un constructor por defecto, es imposible que la opción E sea verdadera. Puesto que el método `main()` está en la misma clase, puede llamar a los métodos `private` en la clase, haciendo que la opción F sea correcta.

**18. D, F.** El método `eat()` es `private` en la clase `Mammal`. Puesto que no se hereda en la clase `Primate`, no está sobrescrito ni sobrecargado, haciendo que las opciones A y B sean incorrectas. El método `drink()` en `Mammal` se oculta correctamente en la clase `Monkey`, ya que la firma es la misma y ambos son `static`, haciendo que la opción D sea correcta y la opción C incorrecta. La versión en la clase `Monkey` lanza una nueva excepción, pero no es verificada (_unchecked_); por lo tanto, está permitida. El método `dance()` en `Mammal` está correctamente sobrecargado en la clase `Monkey` porque las firmas no son las mismas, haciendo que la opción E sea incorrecta y la opción F sea correcta. Para que los métodos se sobrescriban, las firmas deben coincidir exactamente. Finalmente, la línea 12 es una sobrescritura inválida y no compila, ya que `int` no es covariante con `void`, haciendo que las opciones G y H sean ambas incorrectas.

**19. F.** La clase `Reptile` define un constructor, pero no es un constructor sin argumentos. Por lo tanto, el constructor de `Lizard` debe llamar explícitamente a `super()`, pasando un valor `int`. Por esta razón, la línea 9 no compila, y la opción F es la respuesta correcta. Si se corrigiera la clase `Lizard` para llamar al constructor `super()` apropiado, entonces el programa imprimiría `BALizard` en tiempo de ejecución, con el inicializador `static` ejecutándose primero, seguido por el inicializador de instancia y, finalmente, la llamada al método usando el método sobrescrito.

**20. E.** El programa compila y se ejecuta sin problema, haciendo que las opciones de la A a la D sean incorrectas. El método `fly()` se sobrescribe correctamente en cada subclase puesto que la firma es la misma, el modificador de acceso es menos restrictivo y los tipos de retorno son covariantes. Para la covarianza, `Macaw` es un subtipo de `Parrot`, el cual es un subtipo de `Bird`, por lo que los tipos de retorno sobrescritos son válidos. De igual modo, todos los constructores están implementados adecuadamente, con llamadas explícitas a los constructores padre según sea necesario. La línea 19 llama a la versión sobrescrita de `fly()` definida en la clase `Macaw`, ya que la sobrescritura reemplaza el método independientemente del tipo de referencia. Esto resulta en que a `feathers` se le asigne un valor de 3. El objeto `Macaw` es luego moldeado (_cast_) a `Parrot`, lo cual está permitido porque `Macaw` hereda de `Parrot`. La variable `feathers` es visible ya que está definida en la clase `Bird`, y la línea 19 imprime 3, haciendo que la opción E sea la respuesta correcta.

**21. B, G.** Los objetos inmutables no incluyen métodos _setter_, lo que hace que la opción A sea incorrecta. Una clase inmutable debe estar marcada como `final` o contener solo constructores `private`, para que ninguna subclase pueda extenderla y hacerla mutable, haciendo que la opción B sea correcta. Las opciones C y E son incorrectas, ya que las clases inmutables pueden contener tanto variables de instancia como `static`. La opción D es incorrecta, ya que marcar una clase como `static` no es una propiedad de los objetos inmutables. La opción F es incorrecta. Si bien una clase inmutable puede contener solo constructores `private`, esto no es un requisito. Finalmente, la opción G es correcta. Está permitido que el llamador acceda a los datos en elementos mutables de un objeto inmutable, siempre que no tengan la capacidad de modificar estos elementos.

**22. D.** El código compila y se ejecuta sin problema, haciendo que la opción E sea incorrecta. La clase `Child` sobrescribe el método `setName()` y oculta la variable `static` `name` definida en la clase heredada `Person`. Puesto que las variables solo se ocultan, no se sobrescriben, hay dos variables `name` distintas accesibles, dependiendo de la ubicación y el tipo de referencia. La línea 8 crea una instancia de `Child`, la cual es moldeada implícitamente a un tipo de referencia `Person` en la línea 9. La línea 10 usa el tipo de referencia `Child`, actualizando `Child.name` a `Elysia`. La línea 11 usa el tipo de referencia `Person`, actualizando `Person.name` a `Sophia`. Las líneas 12 y 13 llaman ambas al método de instancia `setName()` sobrescrito y declarado en la línea 6. Esto establece `Child.name` a `Webby` en la línea 12 y luego a `Olivia` en la línea 13. Los valores finales de `Child.name` y `Person.name` son `Olivia` y `Sophia`, respectivamente, haciendo que la opción D sea la respuesta correcta.

**23. B.** El programa compila, haciendo que la opción F sea incorrecta. Los constructores son llamados desde la clase hija hacia arriba, pero dado que cada línea de un constructor es una llamada a otro constructor, vía `this()` o `super()`, en última instancia se ejecutan de arriba hacia abajo. En la línea 29, el método `main()` llama al constructor `Fennec()` declarado en la línea 19. **Se debe recordar**, los literales enteros en Java se consideran `int` por defecto. Este constructor llama al constructor `Fox()` definido en la línea 12, el cual a su vez llama al constructor `Fox()` sobrecargado declarado en la línea 11. Puesto que el constructor en la línea 11 no llama explícitamente a un constructor padre, el compilador inserta una llamada al constructor sin argumentos `super()`, el cual existe en la línea 3 de la clase `Canine`. La línea 3 se ejecuta entonces, añadiendo `q` a la salida, y la cadena del compilador se desenrolla. La línea 11 entonces se ejecuta, añadiendo `p`, seguida por la línea 14, añadiendo `z`. Finalmente, la línea 21 se ejecuta y se añade `j`, resultando en un valor final para `logger` de `qpzj` y haciendo que la opción B sea correcta. Para el examen, **se debe recordar** seguir los constructores desde el nivel más bajo hacia arriba para determinar la ruta correcta, pero luego ejecutarlos de arriba hacia abajo utilizando el orden establecido.

**24. C.** El código compila y se ejecuta sin problema, haciendo que las opciones E y F sean incorrectas. Primero, se inicializa la clase, comenzando con la superclase `Antelope` y luego la subclase `Gazelle`. Esto implica invocar las declaraciones de variables `static` y los inicializadores `static`. El programa primero imprime 1, seguido de 8. Luego **se sigue** la ruta de constructores desde el objeto creado en la línea 14 hacia arriba, inicializando cada instancia de clase usando un enfoque de arriba hacia abajo. Dentro de cada clase, los inicializadores de instancia se ejecutan, seguidos de los constructores referenciados. La instancia de `Antelope` se inicializa, imprimiendo 24, seguida por la instancia de `Gazelle`, imprimiendo 93. La salida final es 182493, haciendo que la opción C sea la respuesta correcta.

**25. B, C.** Las clases concretas son, por definición, no abstractas, por lo que la opción A es incorrecta. Una clase concreta debe implementar todos los métodos abstractos heredados, por lo que la opción B es correcta. Las clases concretas pueden ser opcionalmente marcadas como `final`, por lo que la opción C es correcta. La opción D es incorrecta; las clases concretas no necesitan ser inmutables. Una subclase concreta solo necesita sobrescribir el método abstracto heredado, no coincidir exactamente con la declaración. Por ejemplo, se puede usar un tipo de retorno covariante. Por esta razón, la opción E es incorrecta.

**26. D.** Las clases están estructuradas correctamente, pero el cuerpo del método `main()` contiene un error del compilador. El objeto `Orca` es moldeado implícitamente a una referencia `Whale` en la línea 7. Esto está permitido porque `Orca` es una subclase de `Whale`. Al realizar el _cast_, la referencia `whale` en la línea 8 no tiene acceso al método `dive(int... depth)`. Por esta razón, la línea 8 no compila, haciendo que la opción D sea correcta.