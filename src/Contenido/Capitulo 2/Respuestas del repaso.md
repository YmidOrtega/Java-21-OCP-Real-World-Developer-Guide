**1. A, D, G.** La opción A es el operador de igualdad y puede utilizarse tanto en primitivos como en referencias a objetos. Las opciones B y C son operadores aritméticos y no pueden aplicarse a un valor booleano. La opción D es el operador de complemento lógico y se utiliza exclusivamente con valores booleanos. La opción E es el operador de módulo, que solo puede utilizarse con primitivos numéricos. La opción F es un operador de negación/resta y solo puede aplicarse a valores numéricos. Finalmente, la opción G es correcta, ya que es posible hacer _cast_ a una variable booleana dado que `boolean` es un tipo.

**2. A, B, D.** La expresión `apples + oranges` se promueve automáticamente a `int`, por lo que `int` y los tipos de datos que pueden promoverse automáticamente desde `int` funcionarán, haciendo que las opciones A, B y D sean correctas. La opción C no funcionará porque `boolean` no es un tipo de datos numérico. Las opciones E y F no funcionarán sin un _cast_ explícito a un tipo de datos más pequeño.

**3. B, C, D, F.** El código no compilará tal como está, por lo que la opción A no es correcta. El valor `2 * ear` se promueve automáticamente a `long` y no puede almacenarse automáticamente en `hearing`, que es un valor `int`. Las opciones B, C y D resuelven este problema reduciendo el valor `long` a `int`. La opción E no resuelve el problema y de hecho lo empeora al intentar colocar el valor en un tipo de datos más pequeño. La opción F resuelve el problema aumentando el tipo de datos de la declaración para que se permita `long`.

**4. B.** El código compila y se ejecuta sin problemas, por lo que la opción E no es correcta. Este ejemplo tiene una trampa debido al segundo operador de asignación incrustado en la línea 5. La expresión `(wolf=false)` asigna el valor `false` a `wolf` y devuelve `false` para toda la expresión. Dado que `teeth` no es igual a 10, el lado izquierdo devuelve `true`; por lo tanto, el OR exclusivo (`^`) de toda la expresión asignada a `canine` es `true`. La salida refleja estas asignaciones, sin cambios en `teeth`, por lo que la opción B es la única respuesta correcta.

**5. A, C.** Las opciones A y C son correctas, ya que muestran operadores en orden de precedencia creciente o igual. Las opciones B y E están en orden de precedencia decreciente o igual. Las opciones D, F y G no están en orden de precedencia ni creciente ni decreciente. En la opción D, el operador de asignación (`=`) está entre dos operadores unarios, con el operador de multiplicación (`*`) incorrectamente en el lugar de mayor precedencia. En la opción F, el operador de complemento lógico (`!`) tiene el orden de precedencia más alto, por lo que debería ser el último. En la opción G, los operadores de asignación tienen el orden de precedencia más bajo, no el más alto, por lo que los dos últimos operadores deberían ser los primeros.

**6. F.** El código no compila porque la línea 3 contiene un error de compilación. El _cast_ `(int)` se aplica a `fruit`, no a la expresión `fruit+vegetables`. Dado que el operador de _cast_ tiene una precedencia mayor que el operador de suma, se aplica a `fruit`, pero la expresión se promueve a `float`, debido a que `vegetables` es `float`. El resultado no puede devolverse como `long` en el método `addCandy()` sin un _cast_. Por esta razón, la opción F es correcta. Si se añadieran paréntesis alrededor de `fruit+vegetables`, entonces la salida sería 3, 5, 6, y la opción B sería correcta. Recuérdese que convertir números de punto flotante a valores integrales resulta en truncamiento, no en redondeo.

**7. D.** En la primera expresión booleana, `vis` es 2 y `ph` es 7, por lo que esta expresión se evalúa como `true & (true || false)`, lo que se reduce a `true`. La segunda expresión booleana utiliza el operador condicional, y dado que `(vis > 2)` es `false`, el lado derecho no se evalúa, dejando `ph` en 7. En la última asignación, `ph` es 7, y el operador de pre-decremento se aplica primero, reduciendo la expresión a `7 <= 6` y resultando en una asignación de `false`. Por estas razones, la opción D es la respuesta correcta.

**8. A.** El código compila y se ejecuta sin problemas, por lo que la opción E es incorrecta. La línea 7 no produce un error de compilación ya que el operador compuesto aplica _casting_ automáticamente. La línea 5 incrementa `pig` en 1, pero devuelve el valor original de 4 dado que utiliza el operador de post-incremento. Luego se asigna este valor a la variable `pig`, y la operación de incremento se descarta. La línea 7 simplemente reduce el valor de `goat` en 1, resultando en una salida de `4 - 1` y haciendo que la opción A sea la respuesta correcta.

**9. A, D, E.** El código compila sin problemas, por lo que la opción G es incorrecta. En la primera expresión, `a > 2` es `false`, por lo que `b` se incrementa a 5; pero como se usa el operador de post-incremento, se imprime 4, haciendo que la opción D sea correcta. El `--c` no se aplicó, porque solo se evaluó una de las expresiones del lado derecho. En la segunda expresión, `a != c` es `false` ya que `c` nunca se modificó. Dado que `b` es 5 debido a la línea anterior y se usa el operador de post-incremento, `b++` devuelve 5. El resultado se asigna entonces a `b` usando el operador de asignación, sobrescribiendo el valor incrementado para `b` e imprimiendo 5, haciendo que la opción E sea correcta. En la última expresión, los paréntesis no son necesarios, pero la falta de ellos puede hacer que las expresiones ternarias sean difíciles de leer. De las líneas anteriores, `a` es 2, `b` es 5 y `c` es 2. Se puede reescribir esta expresión con paréntesis como `(2 > 5 ? (5 < 2 ? 5 : 2) : 1)`. La segunda expresión ternaria nunca se evalúa ya que `2 > 5` es `false`, y la expresión devuelve 1, haciendo que la opción A sea correcta.

**10. E.** El código no compila debido a un error en la segunda línea, haciendo que la opción E sea correcta. Aunque tanto `height` como `weight` se convierten (_cast_) a `byte`, el operador de multiplicación los promueve automáticamente a `int`, resultando en un intento de almacenar un `int` en una variable `short`.

**11. D.** Primero, `*` y `%` tienen la misma precedencia de operador, por lo que la expresión se evalúa de izquierda a derecha a menos que haya paréntesis presentes. La primera expresión se evalúa como `8 % 3`, lo que deja un resto de 2. La segunda expresión se evalúa de izquierda a derecha ya que `*` y `%` tienen la misma precedencia, y se reduce a `6 % -3`, que es 0. La última expresión se reduce a `5 * 1`, que es 5. Por lo tanto, la salida en la línea 14 es `2, 0, 5`, haciendo que la opción D sea la respuesta correcta.

**12. D.** El prefijo `pre-` indica que la operación se aplica primero y se devuelve el nuevo valor, mientras que el prefijo `post-` indica que se devuelve el valor original antes de la operación. A continuación, el incremento aumenta el valor, mientras que el decremento disminuye el valor. Por estas razones, la opción D es la respuesta correcta.

**13. F.** La primera expresión se evalúa de izquierda a derecha, permitiendo reducirla a `false ^ sunday`, que es `true`, porque `sunday` es `true`. En la segunda expresión, se aplica primero el operador de negación (`!`), reduciendo la expresión a `sunday && true`, que se evalúa como `true`. En la última expresión, ambas variables son `true`, por lo que se reducen a `!(true && true)`, que se reduce aún más a `!true`, es decir, `false`. Por estas razones, la opción F es la respuesta correcta.

**14. B, E, G.** El valor de retorno de una operación de asignación en una expresión es el mismo que el valor de la variable recién asignada. Por esta razón, la opción A es incorrecta y la opción E es correcta. La opción B es correcta, ya que los operadores de igualdad (`==`) y desigualdad (`!=`) se pueden usar ambos con objetos. La opción C es incorrecta, ya que los tipos booleanos y numéricos no son comparables. Por ejemplo, no se puede decir `true == 3` sin un error de compilación. La opción D es incorrecta, ya que los operadores lógicos evalúan ambos lados de la expresión. La opción F es incorrecta, ya que Java no acepta números para valores booleanos. Finalmente, la opción G es correcta, ya que se necesita usar el operador de negación (`-`) para invertir o negar valores numéricos, no el operador de complemento lógico (`!`).

**15. D.** El operador ternario es el único operador que toma tres valores, haciendo que la opción D sea la única elección correcta. Las opciones A, B, C, E y G son todos operadores binarios. Si bien pueden encadenarse en expresiones más largas, cada operación utiliza solo dos valores a la vez. La opción F es un operador unario y toma solo un valor.

**16. B.** La primera línea contiene un error de compilación. El valor 3 se convierte (_cast_) a `long`. El valor `1 * 2` se evalúa como `int` pero se promueve a `long` al sumarse al 3. Intentar almacenar un valor `long` en una variable `int` provoca un error de compilación. Las otras líneas no contienen errores de compilación, ya que almacenan valores más pequeños en tipos de datos más grandes o del mismo tamaño, usando _casting_ en las líneas 2 y 4 para hacerlo. Dado que solo una línea no compila, la opción B es correcta.

**17. C, F.** Los valores iniciales de `ticketsTaken` y `ticketsSold` son 1 y 3, respectivamente. Después de la primera asignación compuesta, `ticketsTaken` se incrementa a 2. El valor de `ticketsSold` aumenta de 3 a 5; dado que se usó el operador de post-incremento, el valor de `ticketsTaken++` devuelve 1 (antes del incremento). En la siguiente línea, `ticketsTaken` se duplica a 4. En la línea final, `ticketsSold` aumenta en 1 a 6. Los valores finales de las variables son 4 y 6, para `ticketsTaken` y `ticketsSold` respectivamente, haciendo que las opciones C y F sean las respuestas correctas. Nótese que la última línea no provoca un error de compilación ya que el operador compuesto convierte automáticamente el operando del lado derecho.

**18. C.** Solo los paréntesis, `( )`, se pueden usar para cambiar el orden de operación en una expresión, haciendo que la opción C sea correcta. Los otros operadores, como `[ ]`, `< >` y `{ }`, no se pueden usar para cambiar el orden de precedencia en Java.

**19. B, F.** El código compila y se ejecuta con éxito, por lo que las opciones G y H son incorrectas. En la línea 5, el operador de pre-incremento se ejecuta primero, por lo que `start` se incrementa a 8, y el nuevo valor se devuelve como el lado derecho de la expresión. El valor de `end` se calcula sumando 8 al valor original de 4, dejando un nuevo valor de 12 para `end` y haciendo que la opción F sea una respuesta correcta. En la línea 6, se está incrementando uno más allá del valor máximo de `byte`. Debido al desbordamiento (_overflow_), esto resultará en un número negativo, haciendo que la opción B sea la respuesta correcta. Incluso si no se conociera el valor máximo de `byte`, se debería haber sabido que el código compila y se ejecuta, y buscar la respuesta para `start` con un número negativo.

**20. A, D, E.** Los operadores unarios tienen el orden de precedencia más alto, haciendo que la opción A sea correcta. El operador de negación (`-`) se usa solo para valores numéricos, mientras que el operador de complemento lógico (`!`) se usa exclusivamente para valores booleanos. Por estas razones, la opción B es incorrecta y la opción E es correcta. Finalmente, los operadores de pre-incremento/pre-decremento devuelven el nuevo valor de la variable, mientras que los operadores de post-incremento/post-decremento devuelven la variable original. Por estas razones, la opción C es incorrecta y la opción D es correcta.