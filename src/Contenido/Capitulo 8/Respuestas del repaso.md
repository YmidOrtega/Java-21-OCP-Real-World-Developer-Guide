**1. A.** Este código es correcto. La línea 8 crea una expresión lambda que verifica si `age` es menor que 5, haciendo que la opción A sea correcta. Como solo hay un parámetro y no especifica un tipo, los paréntesis alrededor del parámetro son opcionales. Las líneas 11 y 13 usan la interfaz `Predicate`, que declara un método `test()`.

**2. C.** La interfaz toma dos parámetros `int`. El código en la línea 7 intenta usarlos como si `h` fuera un `StringBuilder`, haciendo que la opción C sea correcta. Es difícil usar tipos en una lambda cuando están implícitamente especificados. Recuerde verificar la interfaz para el tipo real.

**3. A, C.** Una interfaz funcional puede contener cualquier número de métodos no abstractos, incluyendo `default`, `private`, `static` y `private static`. Por esta razón, la opción A es correcta y la opción D es incorrecta. La opción B es incorrecta, ya que las clases nunca se consideran interfaces funcionales. Una interfaz funcional contiene exactamente un método abstracto, aunque los métodos que tienen firmas coincidentes con métodos `public` en `java.lang.Object` no cuentan para la prueba del método único. Por estas razones, la opción C es correcta. Finalmente, la opción E es incorrecta. Si bien una interfaz funcional **puede** marcarse con la anotación `@FunctionalInterface`, no es obligatorio.

**4. A, F.** La opción B es incorrecta porque no usa la palabra clave `return`. Las opciones C, D y E son incorrectas porque la variable `e` ya está en uso desde la lambda y no **puede** redefinirse. Además, la opción C carece de la palabra clave `return`, y la opción E carece del punto y coma. Por lo tanto, las opciones A y F son correctas.

**5. A, C, E.** Java incluye soporte para tres streams primitivos, junto con numerosas interfaces funcionales que los acompañan: `int`, `double` y `long`. Por esta razón, las opciones C y E son correctas. Además, existe la interfaz funcional `BooleanSupplier`, haciendo que la opción A sea correcta. Java no incluye streams primitivos ni interfaces funcionales relacionadas para otros tipos de datos numéricos, haciendo que las opciones B y D sean incorrectas. La opción F es incorrecta porque `String` no es un primitivo sino un objeto. Solo los primitivos tienen _suppliers_ personalizados.

**6. A, C.** `Predicate<String>` toma una lista de parámetros de un parámetro usando el tipo especificado. Las opciones E y F son incorrectas porque especifican el tipo incorrecto. Las opciones B, D y F son incorrectas porque usan la sintaxis incorrecta para el operador flecha. Esto nos deja con las opciones A y C como respuestas.

**7. E.** Aunque parece que hubo escasez de nombres de variables cuando se escribió este código, sí compila. Los nombres de variables lambda y de métodos pueden ser iguales. El parámetro lambda `x` está dentro del ámbito de cada lambda, por lo que se permite reutilizarlo. El tipo se infiere del método al que llama. La primera lambda asigna `x` a un `String` y la segunda a un `Boolean`. Por lo tanto, la opción E es correcta.

**8. E.** La pregunta comienza con un `UnaryOperator<Integer>`, que toma un parámetro y devuelve un valor del mismo tipo. Por lo tanto, la opción E es correcta, ya que `UnaryOperator` extiende `Function`. Observe que otras opciones ni siquiera compilan porque tienen el número incorrecto de tipos genéricos para la interfaz funcional proporcionada. Debe saber que `BiFunction<T,U,R>` toma tres argumentos genéricos, `BinaryOperator<T>` toma un argumento genérico y `Function<T,R>` toma dos argumentos genéricos.

**9. A, F.** La opción A es correcta y la opción B es incorrecta porque un `Supplier` devuelve un valor mientras que un `Consumer` toma uno y actúa sobre él. La opción C es engañosa. `IntSupplier` sí devuelve un `int`. Sin embargo, la opción pregunta sobre `IntegerSupplier`, que no existe. La opción D es incorrecta porque un `Predicate` devuelve un `boolean`. Sí tiene un método llamado `test()`, haciendo que la opción F sea correcta. Finalmente, la opción E es incorrecta porque `Function` tiene un método `apply()`.

**10. A, B, C.** Dado que el ámbito de `start` y `c` está dentro de la lambda, las variables **pueden** declararse o actualizarse después sin problema, haciendo que las opciones A, B y C sean correctas. La opción D es incorrecta porque establecer `end` impide que sea efectivamente final.

**11. D.** El código no compila porque las lambdas se asignan a `var`. El compilador no tiene suficiente información para determinar que son de tipo `Predicate<String>`. Por lo tanto, la opción D es correcta.

**12. A.** El método `a.compose(b)` llama al parámetro `Function` `b` antes que a la variable `Function` de referencia `a`. En este caso, eso significa que multiplicamos por 3 antes de sumar 4. Esto da un resultado de 7, haciendo que la opción A sea correcta.

**13. E.** Las lambdas solo pueden referenciar variables finales o efectivamente finales. Puede decir que la variable `j` es efectivamente final porque agregar la palabra clave `final` antes no introduciría un error de compilación. Cada vez que se ejecuta la sentencia `else`, la variable se redeclara y sale del ámbito. Por lo tanto, no se reasigna. Del mismo modo, `length` es efectivamente final. No hay errores de compilación, y la opción E es correcta.

**14. B, D.** La opción B es una interfaz funcional válida, que podría asignarse a una referencia `Consumer<Camel>`. Observe que el modificador `final` está permitido en variables en la lista de parámetros. La opción D es correcta, ya que la excepción **se devuelve** como un objeto y no **se lanza**. Esto sería compatible con un `BiFunction` que incluyera `RuntimeException` como su tipo de retorno.
Las opciones A y G son incorrectas porque mezclan formatos de tipo para los parámetros. La opción C no es válida porque la variable `b` **se usa** dos veces. La opción E es incorrecta, ya que una sentencia `return` solo está permitida dentro de llaves (`{}`). La opción F es incorrecta porque la declaración de variable requiere un punto y coma (`;`) después.

**15. A, F.** La opción A es una expresión lambda válida. Si bien `main()` es un método `static`, puede acceder a `age` ya que está usando una referencia a una instancia de `Hyena`, que es efectivamente final en este método. Como `var` no es una palabra reservada, **puede** usarse para nombres de variables. La opción F también es correcta, siendo la variable lambda una referencia a un objeto `Hyena`. La variable **se procesa** usando ejecución diferida en el método `testLaugh()`.
Las opciones B y E son incorrectas; dado que la variable local `age` no es efectivamente final, esto llevaría a un error de compilación. La opción C también causaría un error de compilación, ya que la expresión usa el nombre de variable `p`, que ya está declarado dentro del método. Finalmente, la opción D es incorrecta, ya que esto ni siquiera es una expresión lambda.

**16. C.** Las lambdas no pueden redeclarar variables locales, haciendo que las opciones A y B sean incorrectas. La opción D es incorrecta porque establecer `end` impide que sea efectivamente final. Las lambdas solo pueden referenciar variables finales o efectivamente finales. La opción C compila ya que `chars` no **se usa**.

**17. C.** La línea 8 usa llaves alrededor del cuerpo. Esto significa que la palabra clave `return` y el punto y coma son obligatorios. Como el código no compila, la opción C es la respuesta.

**18. B, F, G.** Podemos eliminar cuatro opciones de inmediato. Las opciones A y C están ahí para confundir; estas interfaces no existen. La opción D es incorrecta porque un `BiFunction<T,U,R>` toma tres argumentos genéricos, no dos. La opción E es incorrecta porque ninguno de los ejemplos devuelve un `boolean`. La declaración en la línea 6 no toma ningún parámetro y devuelve un `String`, por lo que un `Supplier<String>` puede completar el espacio en blanco, haciendo que la opción F sea correcta. La declaración en la línea 7 requiere que reconozca que `Consumer` y `Function`, junto con sus equivalentes binarios, tienen un método `andThen()`. Esto hace que la opción B sea correcta. Finalmente, la línea 8 toma un solo parámetro y devuelve el mismo tipo, que es un `UnaryOperator`. Como los tipos son los mismos, solo **se necesita** un parámetro genérico, haciendo que la opción G sea correcta.

**19. F.** Si bien hay muchas cosas en esta pregunta tratando de confundirlo, observe que no hay opciones sobre que el código no compile. Esto le permite centrarse en las lambdas y las referencias a métodos. La opción A es incorrecta porque un `Consumer` requiere un parámetro. Las opciones B y C están cerca. La sintaxis para la lambda es correcta. Sin embargo, `s` ya está definida como una variable local y, por lo tanto, la lambda no puede redefinirla. Las opciones D y E usan sintaxis incorrecta para una referencia a método. La opción F es correcta.

**20. E.** La opción A no compila porque la segunda sentencia dentro del bloque carece de un punto y coma (`;`) al final. La opción B es una expresión lambda inválida porque `t` **se define** dos veces: en la lista de parámetros y dentro de la expresión lambda. Las opciones C y D carecen de una sentencia `return` y de punto y coma. Las opciones E y F son expresiones lambda válidas, aunque solo la opción E coincide con el comportamiento de la clase `Sloth`. En particular, la opción F solo imprime `Sleep:`, no `Sleep: 10.0`.

**21. A, E, F.** Una interfaz funcional válida es aquella que contiene un único método abstracto, excluyendo cualquier método `public` que ya esté definido en `java.lang.Object`. `Transport` y `Boat` son interfaces funcionales válidas, ya que cada una contiene un único método abstracto: `go()` y `hashCode(String)`, respectivamente. Esto nos da las opciones A y E. Dado que los otros métodos son parte de `Object`, no cuentan como métodos abstractos. `Train` también es una interfaz funcional ya que extiende `Transport` y no define ningún método abstracto adicional. Esto añade la opción F como la respuesta final correcta.
`Car` no es una interfaz funcional porque es una clase abstracta. `Locomotive` no es una interfaz funcional porque incluye dos métodos abstractos, uno de los cuales es heredado. Finalmente, `Spaceship` no es una interfaz válida, y mucho menos una interfaz funcional, porque un método `default` debe proporcionar un cuerpo. Una forma rápida de probar si una interfaz es una interfaz funcional es aplicar la anotación `@FunctionalInterface` y verificar si el código aún compila.
