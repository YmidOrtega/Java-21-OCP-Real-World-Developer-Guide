**1. B, D, E.** `Iguana` no compila porque declara un campo de instancia `age` en la firma del `record` Y una variable estática `age` dentro del cuerpo, generando un conflicto de nombres. `Chameleon` no compila porque los `records` no pueden ser `abstract`. `Newt` no compila porque los `records` no pueden extender otras clases (solo pueden extender `Record` implícitamente). `Gecko` compila, ya que `final` es opcional pero permitido en los `records`. `BeardedDragon` compila porque es válido sobrescribir el método de acceso generado automáticamente. `Reptile` compila porque el constructor compacto sin paréntesis es sintaxis válida.

**2. A, B, D, E.** `TurtleFrog` extiende `Frog` (la cual implementa `CanHop`). Por lo tanto, una referencia de tipo `Frog` puede apuntar a `TurtleFrog` (opción A), una referencia `TurtleFrog` también es válida (opción B), y una referencia `CanHop` es válida por herencia de interfaz (opción D). `var` infiere el tipo como `TurtleFrog` (opción E). `BrazilianHornedFrog` no es supertipo de `TurtleFrog`, por lo que la opción C es incorrecta. `Long` no tiene ninguna relación con `TurtleFrog`, por lo que la opción F es incorrecta.

**3. C.** Los constructores de `enum` no pueden ser `public` ni `protected`. La línea 14 (`public Flavors() {}`) no compila porque declara el constructor del `enum` con modificador `public`. Esta es la única línea que genera un error de compilación, haciendo que la opción C sea la respuesta correcta.

**4. B.** El código compila y se ejecuta sin problema. La variable `c` es de tipo `Armadillo` (inferido por `var`). Al llamar a `System.out.println(c)`, se invoca el método `toString()` del objeto, que es el de la clase `Armadillo` (`return "Cute"`). El polimorfismo garantiza que se use la versión sobrescrita del método, independientemente de la variable de referencia. La respuesta correcta es B.

**5. E.** La clase concreta `Beetle` extiende `Insect` y debe implementar todos los métodos abstractos heredados. De `HasExoskeleton` hereda `getNumberOfSections()` (sin parámetros). La línea 10 declara `getNumberOfSections(int count)`, que es una sobrecarga, no una implementación del método abstracto. Por lo tanto, `Beetle` no implementa `getNumberOfSections()` de la interfaz, y la declaración de la clase (línea 8) no compila.

**6. D, E.** La línea 4 declara un método `abstract` con cuerpo (`{ return 13; }`), lo cual no está permitido en Java. La línea 7 usa `extends` en lugar de `implements` para una clase abstracta que pretende usar una interfaz. Una clase (abstracta o no) no puede extender una interfaz; debe usar `implements`. Estas dos líneas producen errores de compilación. La línea 8 es una sobrecarga válida del método `eatGrass()`.

**7. E.** La interfaz `Aquatic` declara `int getNumOfGills(int p)`, que es implícitamente `public abstract`. La clase `ClownFish` intenta implementarlo en la línea 6 con `int getNumOfGills(int input)`, pero sin el modificador `public`. Esto reduce el nivel de acceso del método heredado de `public` a acceso de paquete, lo cual no está permitido. La línea 5 es una sobrecarga válida (diferente firma), pero la línea 6 no compila.

**8. B, E, G.** La opción A no compila porque `List<Integer>` no es compatible con `List<String>`. La opción C no compila porque `Object` no tiene método `animal()`. La opción D no compila porque hay dos variables con el mismo nombre `i`. La opción F no compila porque el primer campo de `Exhibit` es de tipo `Walrus`, no `List<?>`. La opción H no compila porque `null` no puede usarse en coincidencia de patrones de `record`. La opción B (`List` raw type) es compatible con `List<String>`, y `Object` es supertipo de `String`, por lo que compila e imprime `true`. La opción E usa `var` para inferir ambos tipos, es válida. La opción G usa `CharSequence` para el segundo campo; como `String` implementa `CharSequence`, es compatible y coincide.

**9. A, E, F.** El parámetro `mySnake` es de tipo `Snake`. Se puede pasar cualquier subtipo de `Snake` o el valor `null`. `new Cobra()` es válido porque `Cobra extends Snake` (opción A). `new Snake()` no compila porque `Snake` es abstracta (opción B). `new Object()` y `new String("Snake")` no son subtipos de `Snake`. `new GardenSnake()` es válido porque `GardenSnake` hereda de `Cobra` que hereda de `Snake` (opción E). `null` siempre es asignable a cualquier tipo de referencia (opción F).

**10. B, E.** La interfaz `Walk` tiene `private static List move()`, que al ser privada no **se hereda**. Por lo tanto, `Leopard` puede declarar cualquier tipo de retorno para `move()`. Sin embargo, se debe recordar que `Leopard` no necesita implementar `Walk.move()` porque es privado. `Run extends Walk` y declara `public ArrayList move()`. `Panther implements Run`, por lo que debe implementar `ArrayList move()`. La opción B (`ArrayList` en X) compila para Leopard. La opción E (`ArrayList` en Z) es el tipo de retorno correcto para que Panther implemente el método de Run. Las opciones A y C también compilarían para X, pero los distractores D y E son para Z.

**11. B.** El código compila y se ejecuta sin problema. Los constructores privados son accesibles dentro de la misma clase, por lo que las líneas 12 y 13 compilan. En Java 16+, las clases internas pueden tener miembros `static`, por lo que la línea 6 compila. El método `startMovie()` imprime `butter`, que en el contexto de `Popcorn` se refiere al campo `static int butter = 10`. La salida es `10`, haciendo que la opción B sea correcta.

**12. A, D, E, F.** El `record Body` es un tipo anidado implícitamente `static` dentro de `BoaConstrictor`. Esto significa que `Body` no puede acceder a los miembros de instancia de `BoaConstrictor` (`tail` y `body`), descartando las opciones B y C. Desde dentro del método `hiss()` (método de instancia de `Body`), son accesibles: `counter()` (método de instancia de Body, opción A), `stripes()` (método de acceso del `record`, opción D), `stripes` (campo del `record`, opción E), y `counter` (campo `static` de Body, opción F). Las líneas 15, 17 y 20–22 compilan sin problema.

**13. G.** El código compila correctamente. Sin embargo, `v` es `null`, y la instrucción `switch` en Java lanza una `NullPointerException` cuando se evalúa `null` con un `switch` de expresión de `enum` sin soporte explícito de `null`. Por lo tanto, el código produce una excepción en tiempo de ejecución, haciendo que la opción G sea la respuesta correcta.

**14. A, C, D, E.** Una interfaz sellada puede restringir tanto qué subinterfaces la extienden como qué clases la implementan, haciendo que las opciones A y E sean correctas. La opción B es incorrecta porque una subclase `non-sealed` sí puede ser extendida por clases no listadas en `permits`. La opción C es correcta porque una clase sellada puede ser extendida por una clase `abstract` siempre que use `final`, `sealed` o `non-sealed`. La opción D es correcta ya que `non-sealed` es precisamente uno de los tres modificadores permitidos. La opción F es incorrecta porque las clases selladas sí pueden contener subclases anidadas.

**15. C.** La variable `g` es una instancia de una clase anónima que extiende `Spirit`. Llamar `g.boo()` imprimiría `"Booo!!!"` (opción A incorrecta). La sintaxis `g.super.boo()` no es válida (opción B incorrecta). `new Ghost().boo()` llama al método `static boo()` de `Ghost`, que imprime `"Not scared"` (opción C correcta). Las opciones D y E son incorrectas por sintaxis inválida o por llamar al método incorrecto.

**16. E.** La clase `OstrichWrangler` es una clase anidada `static`. Las clases anidadas `static` no pueden acceder a las variables de instancia de la clase externa. La línea 5 (`return count`) intenta acceder a `count`, que es una variable de instancia de `Ostrich`. Esto genera un error de compilación en la línea 5. Si la clase compilara, se generarían dos archivos de bytecode: `Ostrich.class` y `Ostrich$OstrichWrangler.class`.

**17. E, G.** La línea 5 declara un método de interfaz con cuerpo (`{ return 2; }`) pero sin la palabra clave `default`. Los métodos de interfaz con cuerpo deben ser `default`, `static` o `private`. Esto no compila (opción E). La línea 7 declara un método con acceso `protected`, lo cual no está permitido en interfaces: los métodos de interfaz solo pueden ser `public`, `private` o de paquete de forma implícita. Esto no compila (opción G). Las demás líneas son válidas.

**18. E.** La clase `Diet` es una clase interna no estática de `Deer`. Intentar instanciarla con `new Diet()` dentro del método `static main()` no es válido porque **se requiere** una instancia de `Deer`. Debería ser `new Deer().new Diet()`. Por lo tanto, el método `main()` no compila, haciendo que la opción E sea la respuesta correcta.

**19. F.** El `enum FOOD` declara un método abstracto `isHealthy()`, lo que significa que **cada** valor del `enum` debe proporcionar su propia implementación. Sin embargo, los valores `FISH`, `ROOTS`, `COOKIES` y `HONEY` no implementan `isHealthy()`. Esto genera un error de compilación. La opción F es la respuesta correcta.

**20. H.** La línea 23 intenta hacer una coincidencia de patrones con `Gorilla(int d, double e)`, donde `double` (primitivo) no es compatible con `Double` (tipo envolvente) en el contexto de la coincidencia de patrones de `record`. Java no realiza promoción numérica ni unboxing automático en la coincidencia de patrones. Por lo tanto, la línea 23 genera un error de compilación, haciendo que la opción H (Ninguna de las anteriores) sea correcta.

**21. E.** Un constructor sobrecargado en un `record` debe llamar a `this()` como primera línea. `RabbitFood()` es un constructor sobrecargado que necesita llamar al constructor canónico `this(size, brand, expires)`. La opción A no compila porque intentar asignar `size` en un constructor sobrecargado sin llamar primero a `this()` no es válido. La opción B tampoco funciona porque `this.size` no puede modificarse después de la asignación. La opción D no compila por usar `super`. La opción C sería válida solo como parte del cuerpo después de `this(...)`. La opción E (`throw new RuntimeException()`) es válida como la primera instrucción en un constructor sobrecargado (sin necesidad de `this()`), por lo que compila correctamente.

**22. C, D, G.** El método `rest()` es `static`, por lo que no puede acceder a instancias de clases internas no estáticas sin una referencia explícita. `Cub` es una clase interna no estática, por lo que requiere una instancia de `Lion` para ser instanciada. La opción C (`new Lion().new Cub()`) es válida y compila. La opción D (`new Den()`) compila porque `Den` es una clase anidada `static`. La opción G (`new Lion.Den()`) también compila como alternativa a `new Den()`. Las demás opciones usan sintaxis inválida o intentan instanciar `Cub` sin una referencia a `Lion` en un contexto estático.

**23. D.** Dado que `Penguin` implementa tanto `Swim` como `Dance`, ambas con un método `default perform()`, `Penguin` debe sobrescribir `perform()` (lo cual hace). Para llamar específicamente a la versión de `Swim`, **se usa** la sintaxis `Swim.super.perform()`. La opción A (`super.perform()`) es ambigua y no compilaría correctamente. La opción B (`Swim.perform()`) no es sintaxis válida para métodos de instancia. La opción D es la sintaxis correcta para llamar a un método `default` de una interfaz específica.

**24. B, E.** La línea 3 declara un método `static hunt()` que llama a `getName()`, el cual es un método de instancia (abstracto). Un método `static` no puede llamar a un método no estático sin una referencia de objeto. La línea 3 no compila (opción B). La línea 6 declara `private static boolean sneak()` que llama a `roar()`, el cual es un método `private` no estático. Un método `private static` no puede llamar a un método de instancia directamente. La línea 6 no compila (opción E). Las demás líneas son válidas.

**25. B.** El código compila sin problema. La clase local `Stripes` es `abstract`, lo cual está permitido en clases locales. La línea 11 instancia una clase anónima que extiende `Stripes`. El método `print()` imprime `message + Zebra.this.x`. `message` es `"x is "` y `Zebra.this.x` se refiere a la variable de instancia `x` de la clase `Zebra`, cuyo valor es `24`. La salida es `"x is 24"`, haciendo que la opción B sea correcta.

**26. C.** El `enum Animals` tiene un campo `legs` que no es `final`. La línea 34 (`Animals.BIRD.legs = List.of(-1)`) reasigna este campo (mala práctica pero válida). `BIRD` sobreescribe `stand()` para devolver `legs.get(0) + 4`. Con `legs = List.of(-1)`, `legs.get(0)` es `-1`, por lo que `stand()` devuelve `-1 + 4 = 3`. La opción C es correcta.

**27. B, C, D, G.** El compilador siempre inserta en un `record`: un método de acceso para cada campo (opción B), el método `toString()` (opción C), el método `equals()` (opción D) y el método `hashCode()` (opción G). No **se inserta** un constructor sin argumentos (a menos que se defina explícitamente), ni métodos mutadores (los `records` son inmutables), ni métodos de ordenamiento.

**28. A, B, D.** `Camel` es una clase abstracta con el método `void travel()` sin cuerpo y sin la palabra clave `abstract`. Los métodos sin cuerpo en clases deben ser abstractos (opción A no compila). `EatsGrass` declara `private abstract int chew()`, pero un método `private` no puede ser `abstract` (opción B no compila). `Elephant` contiene una clase interna `abstract private class SleepsAlot`; los modificadores `abstract` y `private` son válidos en clases anidadas (compila). `Eagle` declara `abstract soar()` sin tipo de retorno ni la palabra clave `abstract` en la clase, y la declaración del método carece de tipo de retorno (opción D no compila). `Spider` compila correctamente.

**29. E.** Las líneas 5 y 12 usan `this().age = 3` y `this().age = 6` respectivamente. `this()` llama a otro constructor y devuelve `void`, por lo que no se puede acceder a `.age` desde el resultado de `this()`. Estas dos líneas no compilan, haciendo que haya 2 líneas con errores de compilación (opción E).

**30. B, C, E.** `Bird` compila correctamente; `Flamingo` es una subclase anidada `final`, lo cual es válido y la cláusula `permits` puede omitirse. `Monkey` no compila porque es una clase sellada (`sealed`) sin ninguna subclase declarada en la cláusula `permits` y no hay subclases en el mismo archivo que la extiendan usando la palabra clave `extends`. `EmperorTamarin` no compila porque extiende `Monkey` (clase sellada) pero no está listada en ninguna cláusula `permits` de `Monkey`. `Mandrill` compila porque está en el mismo archivo y `Monkey` puede inferir sus subclases directas. `Friendly` no compila porque extiende `Mandrill` (clase `non-sealed`) pero usa `sealed`, lo que requiere que `Silly` extienda `Friendly`. Sin embargo, `Silly` no extiende `Friendly`. Por lo tanto, las opciones B, C y E son las que contienen errores.
