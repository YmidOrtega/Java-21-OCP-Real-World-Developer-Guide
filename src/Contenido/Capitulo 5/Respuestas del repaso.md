1. **A, E**. Las variables de instancia y estáticas se pueden marcar como `final`, lo que hace que la opción A sea correcta. "Efectivamente final" significa que una variable local no está marcada como `final` pero su valor no cambia después de ser establecido, lo que hace que la opción B sea incorrecta. La opción C es incorrecta, ya que `final` se refiere solo a la referencia de un objeto, no a su contenido. La opción D es incorrecta, ya que `var` y `final` se pueden usar juntos. Finalmente, la opción E es correcta: una vez que un primitivo se marca como `final`, no se puede modificar.

2. **B, C**. La palabra clave `void` es un tipo de retorno. Solo el modificador de acceso o los especificadores opcionales están permitidos antes del tipo de retorno. La opción C es correcta, ya que crea un método con acceso `private`. La opción B también es correcta, ya que crea un método con acceso de paquete y el especificador opcional `final`. Dado que el acceso de paquete no utiliza un modificador, se puede pasar directamente a `final`. La opción A es incorrecta porque el acceso de paquete omite el modificador de acceso en lugar de especificar `default`. La opción D es incorrecta porque Java distingue entre mayúsculas y minúsculas. Habría sido correcta si `public` fuera la opción. La opción E es incorrecta porque el método ya tiene un tipo de retorno `void`. La opción F es incorrecta porque no se permiten etiquetas para los métodos.

3. **A, D**. Las opciones A y D son correctas porque los especificadores opcionales están permitidos en cualquier orden. Las opciones B y C son incorrectas porque cada una tiene dos tipos de retorno. Las opciones E y F son incorrectas porque el tipo de retorno está antes del especificador opcional y el modificador de acceso, respectivamente.

4. **A, B, C, E**. El valor `6` puede ser promovido implícitamente a cualquiera de los tipos primitivos más grandes, lo que hace que las opciones A, C y E sean correctas. También puede ser convertido (autoboxed) a `Integer`, lo que hace que la opción B sea correcta. No puede ser promovido y convertido (autoboxed) al mismo tiempo, lo que hace que las opciones D y F sean incorrectas.

5. **A, C, D**. Las opciones A y C son correctas porque a un método `void` se le permite opcionalmente tener una sentencia `return` siempre que no intente devolver un valor. La opción B no compila porque `null` requiere un objeto de referencia como tipo de retorno. Dado que `int` es primitivo, no es un objeto de referencia. La opción D es correcta porque devuelve un valor `int`. La opción E no compila porque intenta devolver un `double` cuando el tipo de retorno es `int`. Dado que un `double` no puede asignarse a un `int`, tampoco puede devolverse como tal. La opción F no compila porque en realidad no se devuelve ningún valor (falta el valor a devolver para un tipo `int`).
 
6. **A, B, F**. Las opciones A y B son correctas porque el único parámetro _varargs_ es el último parámetro declarado. La opción F es correcta porque no usa ningún parámetro _varargs_. La opción C es incorrecta porque el parámetro _varargs_ no es el último. La opción D es incorrecta porque no se permiten dos parámetros _varargs_ en el mismo método. La opción E es incorrecta porque los `...` para un _varargs_ deben estar después del tipo, no antes.

7. **D, F**. Las opciones D y F son correctas. La opción D pasa el parámetro inicial (`boolean b`) más dos más para convertirse en un arreglo _varargs_ de tamaño 2. La opción F pasa el parámetro inicial más un arreglo de tamaño 2. La opción A no compila porque no pasa el parámetro inicial (requerido). La opción E no compila porque no declara un arreglo correctamente (debería ser `new boolean[] {true, true}`). La opción B crea un arreglo _varargs_ de tamaño 0, y la opción C crea un arreglo _varargs_ de tamaño 1.

8. **D**. La opción D es correcta. Una práctica común es configurar todos los campos para que sean `private` y todos los métodos para que sean `public` (encapsulación). La opción A es incorrecta porque el acceso `protected` permite todo lo que permite el acceso de paquete y, además, permite el acceso a subclases. La opción B es incorrecta porque la clase es `public`. Esto significa que otras clases pueden ver la clase. Sin embargo, no pueden llamar a ninguno de los métodos o leer ninguno de los campos de paquete desde fuera. Es esencialmente una clase inútil. La opción C es incorrecta porque el acceso de paquete se aplica a todo el paquete, no solo a algunas clases en él. La opción E es incorrecta porque Java no tiene tal capacidad de acceso con comodines.

9. **B, C, D, F**. Las dos clases están en paquetes diferentes, lo que significa que el acceso `private` y el acceso de paquete no compilarán desde la otra clase. Esto causa errores del compilador en las líneas 5, 6 y 7, lo que hace que las opciones B, C y D sean respuestas correctas. Además, el acceso `protected` no compilará ya que `School` no hereda de `Classroom`. Esto causa el error del compilador en la línea 9, lo que hace que la opción F también sea una respuesta correcta.
 
10. **B**. `Rope` ejecuta la línea 3, estableciendo `LENGTH` en 5, e inmediatamente después ejecuta el inicializador estático, que lo establece en 10. La línea 5 en la clase `Chimp` llama al método estático normalmente e imprime `swing` y un espacio. La línea 6 también llama al método estático. Java permite llamar a un método estático a través de una variable de instancia, aunque no es recomendable. La línea 7 usa la importación estática en la línea 2 para referenciar `LENGTH` e imprime 10. Por estas razones, la opción B es correcta.

11. **B, E**. La línea 10 no compila porque **no está permitido que los métodos estáticos llamen a métodos de instancia**. Aunque se está llamando a `play()` como si fuera un método de instancia y existe una instancia, Java sabe que `play()` es realmente un método estático y lo trata como tal. Dado que esta es la única línea que no compila, la opción B es correcta. Si se elimina la línea 10, el código imprime `swing-swing`, lo que hace que la opción E sea correcta. No lanza una `NullPointerException` en la línea 17 porque `play()` es un método estático. Java observa el **tipo de referencia** de `rope2` y traduce la llamada a `Rope.play()`.

12. **B**. La prueba para saber si es "efectivamente final" es si el modificador `final` se puede añadir a la variable local y el código sigue compilando. La variable `monkey` declarada en la línea 11 no es efectivamente final porque se modifica en la línea 13 (`monkey++`). Las variables `giraffe` y `name` (la primera `name`) declaradas en las líneas 13 y 14, respectivamente, son efectivamente finales y no se modifican después de que se configuran. La variable `name` (la segunda `name`, en otro bloque de alcance pero eclipsando/reasignando conceptualmente, aunque aquí hay un error lógico de re-declaración en el mismo alcance si estuvieran en el mismo nivel, pero aquí `name` de la 14 solo vive en el `if`, y la `name` de la 17 vive en el método) declarada en la línea 17 no es efectivamente final ya que se modifica en la línea 22. Finalmente, la variable `food` en la línea 18 no es efectivamente final ya que se modifica en la línea 20. Dado que hay dos variables efectivamente finales, la opción B es correcta. _(Nota técnica: la variable `name` de la línea 14 se declara pero no se lee, solo se asigna en la 15, es efectivamente final. La variable `name` de la línea 17 es otra variable distinta que se reasigna en la 22)._

13. **D**. Hay dos detalles que notar en este código. Primero, hay que notar que `RopeSwing` tiene un **inicializador de instancia** y no un inicializador estático. Dado que `RopeSwing` nunca se construye (se instancia con `new`), el inicializador de instancia no se ejecuta. El otro detalle es que `length` (en `Rope`) es `static`. Los cambios desde cualquier objeto (o referencia) actualizan esta variable estática común. La línea `rope1.length = 2` establece la variable estática compartida en 2, luego `rope2.length = 8` establece esa misma variable estática compartida en 8. El código imprime 8, lo que hace que la opción D sea correcta.

14. **E**. Si una variable es `static final`, debe configurarse exactamente una vez, y debe ser en la línea de declaración o en un bloque de inicialización estática. La línea 4 no compila porque `bench` no se configura en ninguna de estas ubicaciones (se intenta configurar en `main`, lo cual es demasiado tarde). La línea 15 no compila porque no está permitido configurar variables `final` después del proceso de carga/inicialización de la clase. La línea 11 no compila porque `name` se configura dos veces: una vez en la declaración y otra en el bloque estático. La línea 12 no compila porque `rightRope` también se configura dos veces (ambas en bloques de inicialización estática diferentes, líneas 8 y 12). Dado que cuatro líneas no compilan, la opción E es correcta.

15. **B**. Las dos formas válidas de hacer esto son `import static java.util.Collections.*;` y `import static java.util.Collections.sort;`, lo que hace que la opción B sea correcta. La opción A es incorrecta porque solo se puede hacer una importación estática sobre miembros estáticos. Las clases como `Collections` requieren una importación regular. La opción C no tiene sentido, ya que los parámetros del método no tienen cabida en una importación. Las opciones D, E y F intentan engañar invirtiendo la sintaxis de `import static`.

16. **E**. El argumento en la línea 17 es un `short`. Puede ser promovido a un `int`, por lo que se invoca a `print(int)` en la línea 5. El argumento en la línea 18 es un `boolean`. Puede ser _autoboxed_ a un `Boolean`, por lo que se invoca a `print(Object)` en la línea 11 (ya que `Boolean` hereda de `Object` y no hay `print(boolean)` o `print(Boolean)` específicos). El argumento en la línea 19 es un `double`. Puede ser _autoboxed_ a un `Double`, por lo que se invoca a `print(Object)` en la línea 11. Por lo tanto, la salida es `int-Object-Object-`, y la respuesta correcta es la opción E.

17. **B**. Dado que Java es paso por valor y la variable en la línea 8 (`value`) nunca se reasigna en el método `main`, se mantiene como 9. En el método `square`, `x` comienza como 9. El valor de `y` se convierte en 81, y luego `x` se establece en `-1` (esto solo afecta a la copia local `x`). La línea 9 establece `result` en 81. Sin embargo, se está imprimiendo `value` (en la línea 10), y eso sigue siendo 9, lo que hace que la opción B sea correcta.

18. **B, D, E**. Dado que Java es paso por valor, asignar un nuevo objeto a `a` dentro del método (`a = new StringBuilder("a")`) no cambia la referencia original del invocador (`s1` en `main` sigue apuntando a "s1"). Llamar a `append()` en `b` sí afecta al invocador porque tanto el parámetro del método (`b`) como el argumento del invocador (`s2`) tienen una referencia al mismo objeto mutable. Finalmente, devolver un valor (`a`) sí pasa la nueva referencia al invocador para su asignación a `s3`. Por estas razones, `s1` es "s1", `s2` es "s2b" y `s3` es "a". Las opciones B, D y E son correctas.

19. **B, C, E**. La variable `value1` es una variable de instancia `final`. Puede configurarse solo una vez: en la declaración de la variable, en un inicializador de instancia o en un constructor. La opción A no compila porque la variable `final` ya se configuró en la declaración (línea 2). La variable `value2` es una variable estática. Tanto los inicializadores de instancia como los estáticos pueden acceder a las variables estáticas y modificarlas, lo que hace que las opciones B y E sean correctas. La variable `value3` es una variable de instancia, por lo que puede modificarse en el inicializador de instancia (fragmento 1), lo que hace que la opción C sea correcta. Las opciones D y F no compilan porque un inicializador estático (fragmento 2) no tiene acceso a variables de instancia (ya que se ejecuta antes de que se creen instancias).

20. **A, E**. El parámetro `100` es un `int` y, por lo tanto, llama al método `int` coincidente, lo que hace que la opción A sea correcta (imprime `2-`). Cuando se elimina este método, Java busca la coincidencia más específica. Java prefiere el _autoboxing_ a los _varargs_, por lo que elige la firma de `Integer` (imprime `3-`). El parámetro `100L` es un `long`. Dado que no se puede convertir a un tipo más pequeño (sin _cast_ explícito), se hace _autoboxing_ en un `Long`, y luego se llama al método para `Object` (ya que `Long` hereda de `Object`), imprimiendo `4-`. Por lo tanto, imprime `2-4-` originalmente, y `3-4-` si se elimina el método `int`, haciendo correctas las opciones A y E.

21. **B, D**. La opción A es incorrecta porque tiene la misma lista de parámetros de tipos y, por lo tanto, la misma firma que el método original. Las opciones B y D son las respuestas correctas, ya que son sobrecargas de métodos válidas en las que los **tipos** de parámetros cambian. Al sobrecargar métodos, el tipo de retorno y los modificadores de acceso no necesitan ser los mismos. Las opciones C y E son incorrectas porque el **nombre** del método es diferente (`moooo` en lugar de `moo`). Las opciones F y G no compilan. Puede haber como máximo un parámetro _varargs_, y debe ser el **último** elemento de la lista de parámetros.