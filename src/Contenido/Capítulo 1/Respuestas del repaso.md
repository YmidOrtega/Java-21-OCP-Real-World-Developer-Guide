**1. D, E.** La opción E es la firma canónica del método `main()`. Es necesario memorizarla. La opción D es una forma alternativa con el modificador `final` redundante. La opción A es incorrecta porque el método `main()` debe ser `public`. Las opciones B y F son incorrectas porque el método `main()` debe tener un tipo de retorno `void`. La opción C es incorrecta porque el método `main()` debe ser `static`.

**2. C, D, E.** Las declaraciones de `package` e `import` son ambas opcionales. Si ambas están presentes, el orden debe ser `package`, luego `import`, y finalmente `class`. La opción A es incorrecta porque `class` está antes de `package` e `import`. La opción B es incorrecta porque `import` está antes de `package`. La opción F es incorrecta porque `class` está antes de `package`.

**3. A, E.** `Bunny` es una clase, lo cual se puede ver en la declaración: `public class Bunny`. La variable `bun` es una referencia a un objeto. El método `main()` es el punto de entrada estándar a un programa. La opción G es incorrecta porque lo que importa es el tipo del parámetro, no el nombre del parámetro.

**4. B, E, G.** La opción A no es válida porque no se permite un guion bajo solitario. La opción C no es un identificador válido porque `true` es una palabra reservada de Java. La opción D no es válida porque no se permite un punto (`.`) en los identificadores. La opción F no es válida porque el primer carácter no es una letra, un signo de dólar (`$`) o un guion bajo (`_`). Las opciones B, E y G son válidas porque contienen solo caracteres permitidos.

**5. A, D, F.** Nunca se garantiza que la recolección de basura se ejecute, lo que hace que la opción F sea correcta y la opción E incorrecta. Además, la clase compila y se ejecuta sin problemas, por lo que la opción G es incorrecta. El objeto `Bear` creado en la línea 9 es accesible hasta la línea 13 a través de la variable de referencia `brownBear`, lo cual corresponde a la opción A. El objeto `Bear` creado en la línea 10 es accesible a través de la referencia `polarBear` y la referencia `brownBear.pandaBear`. Después de la línea 12, el objeto sigue siendo accesible a través de `brownBear.pandaBear`. Sin embargo, después de la línea 13, ya no es accesible dado que `brownBear` deja de ser accesible, lo que hace que la opción D sea la respuesta correcta final.

**6. F.** Para resolver este problema, es necesario rastrear las llaves (`{}`) y observar cuándo las variables entran y salen del alcance (_scope_). Las variables en las líneas 2 y 7 solo están en alcance para un bloque de una sola línea. La variable en la línea 12 solo está en alcance para el bucle `for`. Ninguna de estas está en alcance en la línea 14. Por el contrario, las tres variables de instancia en las líneas 3 y 4 están disponibles en todos los métodos de instancia. Adicionalmente, las variables en las líneas 6, 9 y 10 están disponibles ya que el método y el bucle `while` siguen en alcance. Esto suma un total de 7 variables, que corresponde a la opción F.

**7. C, E.** Lo primero que se debe reconocer es que esto es un bloque de texto y el código dentro de las `"""` es simplemente texto. Las opciones A y B son incorrectas porque las variables `numForks` y `numKnives` no se utilizan. Esto es conveniente, ya que `numKnives` no está inicializada y no compilaría si fuera referenciada. La opción C es correcta ya que coincide con el texto. La opción D es incorrecta porque el bloque de texto no tiene una línea en blanco final. Finalmente, la opción E también es una respuesta correcta ya que `" # knives` está indentado.

**8. B, D, E, H.** Una variable `var` no puede inicializarse con un valor `null` sin un tipo, pero se le puede asignar un valor `null` más tarde si el tipo subyacente no es un primitivo. Por estas razones, la opción H es correcta, pero las opciones A y C son incorrectas. Las opciones B y D son correctas ya que los tipos subyacentes son `String` e `Integer`, respectivamente. La opción E es correcta ya que es una expresión numérica válida. Es posible que se sepa que dividir por cero produce una excepción en tiempo de ejecución, pero la pregunta era solo si el código compilaba. Finalmente, las opciones F y G son incorrectas ya que `var` no se puede utilizar en una asignación de múltiples variables.

**9. E.** Las opciones C y D son incorrectas porque las variables locales no tienen valores predeterminados. La opción A es incorrecta porque `float` debería tener un punto decimal (o sufijo `f`). La opción B es incorrecta porque los primitivos no tienen `null` como valor predeterminado. La opción E es correcta y la opción F es incorrecta porque los tipos de referencia en variables de clase tienen `null` como valor predeterminado.

**10. A, E, F.** Se puede colocar un guion bajo (`_`) en cualquier literal numérico, siempre que no esté al principio, al final o junto a un punto decimal (`.`). Incluso se pueden colocar guiones bajos uno al lado del otro. Por estas razones, las opciones A, E y F son correctas. Las opciones B y D son incorrectas ya que el guion bajo (`_`) está junto a un punto decimal (`.`). Las opciones C y G son incorrectas porque no se puede colocar un guion bajo (`_`) al principio o al final del literal.

**11. E.** Las dos primeras importaciones se pueden eliminar porque `java.lang` se importa automáticamente. Las siguientes dos importaciones se pueden eliminar porque `Tank` y `Water` están en el mismo paquete, lo que hace que la opción E sea la correcta. Si `Tank` y `Water` estuvieran en paquetes diferentes, exactamente una de estas dos importaciones podría eliminarse. En ese caso, la respuesta sería la opción D.

**12. A, C, D.** La línea 2 no compila ya que solo se debe especificar un tipo, lo que hace que la opción A sea correcta. La línea 3 compila sin problemas, ya que declara una variable local dentro de un inicializador de instancia que nunca se usa. La línea 4 no compila porque Java no admite el establecimiento de valores predeterminados para parámetros de método, lo que hace que la opción C sea correcta. Finalmente, la línea 7 no compila porque `fins` está en alcance y es accesible solo dentro del inicializador de instancia en la línea 3, lo que hace que la opción D sea correcta.

**13. A, B, C.** La opción A es correcta porque importa todas las clases en el paquete `aquarium`, incluyendo `aquarium.Water`. Las opciones B y C son correctas porque importan `Water` por nombre de clase. Dado que importar por nombre de clase tiene prioridad sobre los comodines, estas compilan. La opción D es incorrecta porque Java no sabe cuál de las dos clases `Water` de los comodines debe usar. La opción E es incorrecta porque no se puede especificar el mismo nombre de clase en dos importaciones.

**14. A, B, D, E.** La línea 3 no compila porque el sufijo `L` convierte el valor literal en `long`, el cual no puede almacenarse directamente dentro de un `short`, lo que hace que la opción A sea correcta. La línea 4 no compila porque `int` es un tipo entero, pero `2.0` es un valor literal `double`, lo que hace que la opción B sea correcta. La línea 5 compila sin problemas. Las líneas 6 y 7 no compilan porque `numPets` y `numGrains` son ambos primitivos, y solo se pueden invocar métodos en tipos de referencia, no en valores primitivos, lo que hace que las opciones D y E sean correctas, respectivamente. Finalmente, la línea 8 compila porque hay un método `length()` definido en `String`.

**15. C, E, F.** En Java, no hay garantías sobre cuándo se ejecutará la recolección de basura. La JVM es libre de ignorar las llamadas a `System.gc()`. Por esta razón, las opciones A, B y D son incorrectas. La opción C es correcta ya que el propósito de la recolección de basura es reclamar memoria utilizada. La opción E también es correcta en cuanto a que un objeto puede nunca ser recolectado, por ejemplo, si el programa termina antes de que se ejecute la recolección. La opción F es correcta y es el medio principal por el cual los algoritmos de recolección de basura determinan si un objeto es elegible. Finalmente, la opción G es incorrecta ya que marcar una variable como `final` significa que es constante dentro de su propio alcance. Por ejemplo, una variable local marcada como `final` será elegible para la recolección de basura después de que el método termine, asumiendo que no existen otras referencias al objeto fuera del método.

**16. A, D.** La opción A es correcta. Hay dos líneas. Una comienza con `squirrel` y la otra comienza con `pigeon`. Recuérdese que una barra invertida significa omitir el salto de línea. La opción D también es correcta ya que `\s` significa mantener el espacio en blanco. En un bloque de texto, la indentación incidental se ignora, lo que hace que la opción F sea incorrecta.

**17. D, F, G.** El código compila y se ejecuta sin problemas, por lo que las opciones A y B son incorrectas. Un campo `boolean` se inicializa en `false`, lo que hace que la opción D sea correcta imprimiendo `Empty = false`. Las referencias a objetos se inicializan en `null`, no en la cadena vacía, por lo que la opción F es correcta imprimiendo `Brand = null`. Finalmente, el valor predeterminado de los números de punto flotante es `0.0`. Aunque los valores `float` pueden declararse con un sufijo `f`, no se imprimen con dicho sufijo. Por estas razones, la opción G es correcta y se imprime `Code = 0.0`.

**18. B, C, F.** Una variable `var` no se puede usar como parámetro de constructor o método, ni como variable de instancia o de clase, lo que hace que la opción A sea incorrecta y la opción C correcta. El tipo de una `var` se conoce en tiempo de compilación y no puede cambiarse en tiempo de ejecución, aunque su valor sí puede cambiar. Por estas razones, las opciones B y F son correctas y la opción E es incorrecta. La opción D es incorrecta, ya que `var` no se permite en declaraciones de múltiples variables. Finalmente, la opción G es incorrecta, ya que `var` no es una palabra reservada en Java.

**19. A, D.** Las primeras dos líneas proporcionan una forma de convertir un `String` en un número. El primero es un primitivo `int`, y el segundo es un objeto de referencia `Integer`, lo que hace que la opción D sea una de las respuestas. Recuérdese que `B` es 11 en base 16. El código es correcto y el máximo es 11, lo que corresponde a la opción A.

**20. C.** El punto clave a notar es que la línea 4 no define un constructor, sino un método llamado `PoliceBox()`, ya que tiene un tipo de retorno `void`. Este método nunca se ejecuta durante la ejecución del programa, y `color` y `age` se asignan con los valores predeterminados `null` y `0L`, respectivamente. Las líneas 11 y 12 cambian los valores para un objeto asociado con `p`, pero luego, en la línea 13, la variable `p` se cambia para apuntar al objeto asociado con `q`, que todavía tiene los valores predeterminados. Por esta razón, el programa imprime `Q1=null`, `Q2=0`, `P1=null` y `P2=0`, lo que hace que la opción C sea la única respuesta correcta.

**21. D.** Se comienza con el método `main()`, que imprime `7-` en la línea 10. A continuación, se crea una nueva instancia de `Salmon` en la línea 11. Esto provoca que los dos inicializadores de instancia en las líneas 3 y 4 se ejecuten en orden. El valor predeterminado de una variable de instancia de tipo `int` es 0, por lo que se imprime `0-` a continuación, y `count` se asigna un valor de 1. Luego, se llama al constructor. Esto asigna un valor de 4 a `count` e imprime `2-`. Finalmente, la línea 12 imprime `4-`, ya que ese es el valor de `count`. Juntando todo, se tiene `7-0-2-4-`, lo que hace que la opción D sea la respuesta correcta.

**22. C, F, G.** Primero, `0b` es el prefijo para un valor binario y `0x` es el prefijo para un valor hexadecimal. Estos valores se pueden asignar a muchos tipos primitivos, incluidos `int` y `double`, lo que hace que las opciones C y F sean correctas. La opción A es incorrecta porque nombrar la variable `Amount` (con mayúscula) causará que la llamada `System.out.print(amount)` en la siguiente línea no compile (por diferencia de mayúsculas). La opción B es incorrecta porque `9L` es un valor `long`. Si el tipo se cambiara a `long amount = 9L`, entonces compilaría. La opción D es incorrecta porque `1_2.0` es un valor `double`. Si el tipo se cambiara a `double amount = 1_2.0`, entonces compilaría. Las opciones E y H son incorrectas porque el guion bajo (`_`) aparece junto al punto decimal (`.`), lo cual no está permitido. Finalmente, la opción G es correcta, y el uso del guion bajo y la asignación son válidos.

**23. A, D.** El primer error de compilación está en la línea 3. La variable `temp` se declara como `float`, pero el valor asignado es `50.0`, que es un `double` sin el sufijo `F` o `f`. Dado que un `double` no cabe dentro de un `float`, la línea 3 no compila. A continuación, `depth` se declara dentro del bucle `for` y tiene alcance solo dentro de este bucle. Por lo tanto, leer el valor en la línea 10 provoca un error de compilación. Por estas razones, las opciones A y D son las respuestas correctas.