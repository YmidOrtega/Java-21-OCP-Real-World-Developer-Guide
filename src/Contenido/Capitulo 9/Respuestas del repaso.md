**1. A, F.** La opción E es incorrecta para ambos escenarios, ya que `SequencedTreeSet` no existe. Para el primer escenario, la respuesta necesita implementar `List` porque el escenario permite duplicados, reduciéndolo a las opciones A y D. La opción A es mejor que la opción D porque `LinkedList` es tanto una `List` como una `Queue`, y solo **se necesita** una `List` normal. Para el segundo escenario, la respuesta necesita implementar `Map` porque **se trabaja** con pares clave/valor según el campo de ID único. Esto lo reduce a las opciones B y F. Dado que la pregunta habla sobre ordenación, **se necesita** el `TreeMap`. Por lo tanto, la respuesta es la opción F.

**2. C, G.** La línea 12 crea un `List<?>`, lo que significa que **se trata** como si todos los elementos fueran de tipo `Object` en lugar de `String`. Las líneas 15 y 16 no compilan ya que llaman a los métodos `isEmpty()` y `length()` de `String`, que no están definidos en `Object`. La línea 13 crea un `List<String>` porque `var` usa el tipo que deduce del contexto. Las líneas 17 y 18 sí compilan. Sin embargo, `List.of()` crea una lista inmutable, por lo que ambas líneas lanzarían una `UnsupportedOperationException` si **se ejecutaran**. Por lo tanto, las opciones C y G son correctas.

**3. B.** Esta es una cola de doble extremo. En las líneas 4 y 5, agregamos al final, dando `[hello, hi]`. En la línea 6, agregamos al frente y tenemos `[ola, hello, hi]`. En la línea 7, eliminamos el primer elemento, que es `"ola"`. En la línea 8, observamos el nuevo primer elemento (`"hello"`) pero no lo eliminamos. En las líneas 9 y 10, eliminamos cada elemento por turno hasta que no quedan elementos, imprimiendo `hello` y `hi` juntos, lo que hace que la opción B sea la respuesta.

**4. B, F.** La opción A no compila porque los tipos genéricos no son compatibles. Podríamos decir `HashSet<? extends Number> hs2 = new HashSet<Integer>();`. La opción B usa un límite inferior, por lo que permite tipos genéricos de superclase. La opción C no compila porque el operador diamante solo está permitido en el lado derecho. La opción D no compila porque un `Set` no es un `List`. La opción E no compila porque los límites superiores no están permitidos al instanciar el tipo. Finalmente, la opción F sí compila porque el límite superior está en el lado correcto del `=`.

**5. B.** El _record_ compila y **se ejecuta** sin problemas. La línea 8 da una advertencia del compilador por no usar genéricos, pero no un error de compilación. La línea 7 crea la clase `Hello` con el tipo genérico `String`. También pasa un `int` al método `println()`, que **se autoboxea** en un `Integer`. Si bien el método `println()` toma un parámetro genérico de tipo `T`, no es el mismo `<T>` definido para la clase en la línea 1. En cambio, es un `T` diferente definido como parte de la declaración del método en la línea 3. Por lo tanto, el argumento `String` en la línea 7 aplica solo a la clase. El método puede tomar cualquier objeto como parámetro, incluidos primitivos autboxeados. La línea 8 crea la clase `Hello` con el tipo genérico `Object` ya que no **se especifica** ningún tipo para esa instancia. Pasa un `boolean` a `println()`, que **se autoboxea** en un `Boolean`. El resultado es que `hi-1hola-true` **se imprime**, haciendo que la opción B sea correcta.

**6. B, F.** Buscamos una definición de `Comparator` que ordene en orden descendente por `beakLength`. La opción A es incorrecta porque ordena en orden ascendente por `beakLength`. Del mismo modo, la opción C es incorrecta porque ordena por `beakLength` en orden ascendente dentro de aquellas coincidencias que tienen el mismo `name`. La opción E es incorrecta porque no existe un método `thenComparingNumber()`.
La opción B es una respuesta correcta, ya que ordena por `beakLength` en orden descendente. Las opciones D y F son más complicadas. Primero, observe que podemos llamar a `thenComparing()` o `thenComparingInt()` porque el primero simplemente autoboxeará el `int` en un `Integer`. Luego observe a qué **se aplica** `reversed()`. La opción D es incorrecta porque ordena por `name` en orden ascendente y solo invierte el `beakLength` de aquellos con el mismo `name`. La opción F crea un comparador que ordena por `name` en orden ascendente y luego por `beakSize` en orden ascendente. Finalmente, invierte el resultado. Esto es justo lo que queremos, así que la opción F es correcta.

**7. B, F.** Una sobrescritura válida de un método con argumentos genéricos debe tener un tipo de retorno que sea covariante, con parámetros de tipo genérico coincidentes. Las opciones D y E son incorrectas porque el tipo de retorno es demasiado amplio. Además, los argumentos genéricos deben tener la misma firma con los mismos tipos genéricos. Esto elimina las opciones A y C. Las opciones restantes son correctas, haciendo que la respuesta sean las opciones B y F.

**8. E.** No hay un método `get(int)` definido en `SequencedCollection`, lo que significa que la línea 16 no compila, independientemente de lo que **se coloque** en el espacio en blanco. Por esta razón, la opción E es correcta. Si la línea 16 **se eliminara** o corrigiera para usar `getFirst()`, entonces `LinkedList` sería la respuesta correcta.

**9. A.** El arreglo **se ordena** usando `MyComparator`, que ordena los elementos en orden alfabético inverso sin distinción de mayúsculas y minúsculas. Normalmente, los números se ordenan antes que las letras. Este código invierte eso llamando al método `compareTo()` en `b` en lugar de `a`. Por lo tanto, la opción A es correcta.

**10. A, B, D.** El tipo genérico debe ser `Exception` o una subclase de `Exception` ya que esto es un límite superior, haciendo que las opciones A y B sean correctas. Las opciones C y E son incorrectas porque `Throwable` es una superclase de `Exception`. Además, la opción D es correcta a pesar de la sintaxis extraña al enumerar explícitamente el tipo. Aun así debería poder reconocerlo como aceptable.

**11. A, B, E, F.** El método `forEach()` funciona con una `Collection`, como `List` o `Set`. Por lo tanto, las opciones A y B son correctas. Además, las opciones E y F devuelven un `Set` y una `Collection`, respectivamente, y también **pueden usarse**. Las opciones D y G se refieren a métodos que no existen. La opción C es engañosa porque un `Map` sí tiene un método `forEach()`. Sin embargo, usa dos parámetros lambda en lugar de uno. Como no hay un método `System.out.println` coincidente, no compila.

**12. B, E.** El método `showSize()` puede tomar cualquier tipo de `List` ya que usa un comodín no acotado (_unbounded wildcard_). La opción A es incorrecta porque es un `Set` y no un `List`. La opción C es incorrecta porque el comodín no está permitido en el lado derecho de una asignación. La opción D es incorrecta porque los tipos genéricos no son compatibles.
La opción B es correcta porque un comodín de límite inferior permite que ese mismo tipo sea el genérico. La opción E es correcta porque `Integer` es una subclase de `Number`.

**13. C.** Esta pregunta es difícil porque define tanto `Comparable` como `Comparator` en el mismo objeto. El objeto `t1` no especifica un `Comparator`, por lo que usa el método `compareTo()` del objeto `Comparable`. Esto ordena por la variable de instancia `text`. El objeto `t2` sí especifica un `Comparator` al llamar al constructor, por lo que usa el método `compare()`, que ordena por el `int`. Esto nos da la opción C como respuesta. Observe que la referencia `SequencedSet` en la línea 16 no cambia el orden, ya que el objeto subyacente sigue siendo un `TreeSet`.

**14. A.** Al usar `binarySearch()`, la `List` debe estar ordenada en el mismo orden que usa el `Comparator`. Dado que el método `binarySearch()` no especifica un `Comparator` explícitamente, **se usa** el orden de clasificación predeterminado. Solo `c2` usa ese orden de clasificación e identifica correctamente que el valor 2 está en el índice 0. Por lo tanto, la opción A es correcta. Los otros dos comparadores ordenan en orden descendente. Por lo tanto, la condición previa para `binarySearch()` no **se cumple**, y el resultado no está definido para esos dos. Las dos llamadas a `reverse()` están ahí solo para distraer; se cancelan mutuamente.

**15. A, B.** `Y` es tanto una clase como un parámetro de tipo. Esto significa que dentro de la clase `Z`, cuando nos referimos a `Y`, usa el parámetro de tipo. Todas las opciones que mencionan la clase `Y` son incorrectas porque ya no significa la clase `Y`. Solo las opciones A y B son correctas.

**16. A, C.** Un `LinkedList` implementa tanto `List` como `Queue`. La interfaz `List` tiene un método para eliminar por índice. Dado que este método existe, Java no autoboxea para llamar al otro método, haciendo que la salida sea `[10]` y la opción A correcta. Del mismo modo, la opción C es correcta porque el método para eliminar un elemento por índice está disponible en un `LinkedList<Object>` (que es lo que `var` representa aquí). Por el contrario, `Queue` solo tiene el método de eliminar por objeto, por lo que Java sí autoboxea allí. Dado que el número 1 no está en la lista, Java no elimina nada para la `Queue`, y la salida es `[10, 12]`. La llamada a `unmodifiableCollection()` es un distractor ya que es una vista no modificable y la conexión subyacente **se puede** cambiar.

**17. E.** Esta pregunta parece sobre genéricos, pero no lo es. Intenta ver si notó que `Map` no tiene un método `contains()`. Tiene métodos `containsKey()` y `containsValue()` en su lugar, haciendo que la opción E sea la respuesta. Si **se llamara** a `containsKey()`, la respuesta sería `false` porque `123` es una clave `Integer` en el `Map`, en lugar de un `String`.

**18. A, E.** La clave de esta pregunta es llevar la cuenta de los tipos. La línea 48 es un `Map<Integer, Integer>`. La línea 49 construye una `List` a partir de un `Set` de objetos `Entry`, dando `List<Entry<Integer, Integer>>`. Esto causa un error de compilación en la línea 56 ya que no podemos multiplicar un objeto `Entry` por dos.
Las líneas 51–54 son todas de tipo `List<Integer>`. Las primeras tres son inmutables, y la de la línea 54 es mutable. Esto significa que la línea 57 lanza una `UnsupportedOperationException` ya que intentamos modificar la lista. La línea 58 funcionaría si pudiéramos llegar a ella. Dado que hay un error de compilación y un error de tiempo de ejecución, las opciones A y E son correctas.

**19. B.** Al usar tipos genéricos en un método, la especificación genérica va antes del tipo de retorno, por lo que la opción B es correcta.

**20. F.** La primera llamada a `merge()` llama a la función de mapeo y suma los números para obtener 13. Luego actualiza el mapa. La segunda llamada a `merge()` ve que el mapa actualmente tiene un valor `null` para esa clave. No llama a la función de mapeo sino que la reemplaza con el nuevo valor de 3. Por lo tanto, la opción F es correcta.

**21. B, D, F.** La interfaz `java.lang.Comparable` **se implementa** en el objeto para comparar. Especifica el método `compareTo()`, que toma un parámetro. La interfaz `java.util.Comparator` especifica el método `compare()`, que toma dos parámetros. Esto nos da las opciones B, D y F como respuestas.

**22. B.** El código compila y **se ejecuta** sin problemas, por lo que las opciones D y E son incorrectas. Un `TreeMap` ordena sus elementos en el orden natural de las claves (no los valores). Por lo tanto, las líneas 25 y 27 eliminan `[1, Minnie Mouse]` y `[2, Sugar]`, respectivamente. La línea 26 no tiene impacto en el mapa. En la línea 28, `Snowball` **se imprime**, haciendo que la opción B sea correcta. Si la línea 26 **se cambiara** para usar `pollLastEntry()`, entonces el mapa estaría vacío y la línea 28 lanzaría una `NullPointerException` al intentar llamar a `getValue()`.

**23. H.** `TreeSet` es un `SequencedSet`, por lo que sí tiene un método `addFirst()`. Por esta razón, el código compila. Desafortunadamente, `addFirst()` no es compatible en tiempo de ejecución, ya que insertar un elemento al frente del `TreeSet` podría violar el `Comparator` del `TreeSet`. Por esta razón, el programa lanza una `UnsupportedOperationException` en la tercera línea.
