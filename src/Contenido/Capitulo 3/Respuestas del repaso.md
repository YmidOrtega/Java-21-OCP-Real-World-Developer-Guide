**1. F.** Las líneas 34 y 35 no compilan porque les falta el tipo de variable de patrón (_pattern variable type_), lo que hace que la opción F sea correcta. Si se añadiera un tipo soportado, como `Integer`, entre el `case` y la variable en cada línea, entonces el código compilaría y mostraría 4.

**2. A, B, C.** Una expresión `switch` soporta valores `enum`, haciendo que la opción A sea correcta. También soporta primitivos `int` y `byte`, incluyendo sus clases envolventes (_wrapper classes_) `Integer` y `Byte`, haciendo que las opciones B y C sean correctas. No soporta los otros tipos.

**3. B.** El código compila y se ejecuta sin problemas, por lo que las opciones D, E y F son incorrectas. Aunque dos sentencias `else` consecutivas en las líneas 7 y 8 parecen un poco extrañas, están asociadas con sentencias `if` separadas en las líneas 5 y 6, respectivamente. El valor de `humidity` en la línea 4 es igual a `-4 + 12`, que es 8. La primera sentencia `if` se evalúa como verdadera en la línea 5, por lo que la línea 6 se ejecuta y se evalúa como falsa. Esto hace que la sentencia `else` en la línea 7 se ejecute, imprimiendo `Just Right` y haciendo que la opción B sea la respuesta correcta.

**4. A, D, F.** Un bucle `for-each` soporta arreglos, haciendo que las opciones A y F sean correctas. Para `Double[][]`, cada elemento del bucle `for-each` sería un `Double[]`. Un `for-each` también soporta clases que implementan `java.lang.Iterable`. Aunque esto incluye muchas de las clases del _Collections Framework_, no todas implementan `java.lang.Iterable`. Por esta razón, la opción C es incorrecta y la opción D es correcta. Las opciones B, E y G son incorrectas, ya que no implementan `java.lang.Iterable`. Aunque un `String` es una lista de caracteres ordenados, la clase no implementa la interfaz requerida para un bucle `for-each`.

**5. F.** El código no compila porque la expresión `switch` requiere que se manejen todos los valores `case` posibles (exhaustividad), haciendo que la opción F sea correcta. Si se añadiera una cláusula `default` válida, entonces el código compilaría e imprimiría `Turtle` en tiempo de ejecución.

**6. E.** El segundo bucle `for-each` contiene un `continue` seguido de una sentencia `print()`. Debido a que el `continue` no es condicional y siempre se incluye como parte del cuerpo del bucle `for-each`, la sentencia `print()` no es alcanzable (_unreachable_). Por esta razón, la sentencia `print()` no compila. Como este es el único error de compilación, la opción E es correcta. Las otras líneas de código compilan sin problemas.

**7. B, D.** La opción A es incorrecta porque en la primera iteración, intenta acceder a `weather[weather.length]` del arreglo no vacío, lo que hace que se lance una `ArrayIndexOutOfBoundsException`. La opción B es correcta e imprimirá los elementos en orden. La opción C no compila ya que `i` no está definida en `weather[i]`. Para que esto funcionara, el cuerpo del bucle `for-each` también tendría que actualizarse. La opción D también es correcta y es una forma común de imprimir los elementos de un arreglo en orden inverso. La opción E no compila y, por lo tanto, es incorrecta. Se pueden declarar múltiples elementos en un bucle `for`, pero el tipo de datos debe listarse solo una vez, como en `for (int i=0, j=3; …)`. Finalmente, la opción F es incorrecta porque se salta el primer elemento del arreglo. Dado que la expresión condicional se verifica **antes** de que el bucle se ejecute por primera vez, el primer valor de `i` utilizado dentro del cuerpo del bucle será 1.

**8. G.** Las dos primeras sentencias de coincidencia de patrones (_pattern matching_) compilan sin problemas. Se permite que la variable `bat` se use de nuevo, siempre que ya no esté en alcance (_scope_). Sin embargo, la línea 36 no compila. Debido al alcance de flujo (_flow scoping_), si `o` no es un `Long`, entonces `bat` no está en alcance en la expresión `bat <= 20`. La línea 38 tampoco compila ya que `default` no puede usarse como parte de una sentencia `if/else`. Por estas dos razones, la opción G es correcta.

**9. B, C, E.** El código contiene un bucle anidado y una expresión condicional que se ejecuta si la suma de `col + row` es un número par; de lo contrario, `count` se incrementa. Nótese que las opciones E y F son equivalentes a las opciones B y D, respectivamente, ya que las sentencias sin etiqueta se aplican al bucle más interno. Analizando los bucles, la primera vez que la condición es verdadera es en la segunda iteración del bucle interno, cuando `row` es 1 y `col` es 1. La opción A es incorrecta porque esto hace que el bucle salga inmediatamente con `count` establecido solo en 1. Las opciones B, C y E siguen el mismo camino. Primero, `count` se incrementa a 1 en el primer bucle interno, y luego se sale del bucle interno. En la siguiente iteración del bucle externo, `row` es 2 y `col` es 0, por lo que la ejecución sale del bucle interno inmediatamente (porque `2+0=2` que es par). En la tercera iteración del bucle externo, `row` es 3 y `col` es 0, por lo que `count` se incrementa a 2 (porque `3+0=3` impar). En la siguiente iteración del bucle interno, la suma es par (`3+1=4`), por lo que se sale, y el programa se completa, haciendo que las opciones B, C y E sean todas correctas. Las opciones D y F son ambas incorrectas, ya que hacen que los bucles interno y externo se ejecuten múltiples veces, con `count` teniendo un valor de 5 al terminar. No es necesario rastrear todas las iteraciones; basta con detenerse cuando el valor de `count` exceda 2.

**10. E.** Este código contiene numerosos errores de compilación, haciendo que las opciones A y H sean incorrectas. La línea 15 no compila, ya que `continue` no puede usarse dentro de una sentencia `switch` de esta manera. La línea 16 no es una constante en tiempo de compilación ya que cualquier valor `int` puede pasarse como parámetro; marcarlo como `final` no cambia esto, por lo que no compila. La línea 18 no compila porque `Sunday` no está marcada como `final`. Ser "efectivamente final" (_effectively final_) es insuficiente para un `case`. Finalmente, la línea 19 no compila porque `DayOfWeek.MONDAY` no es un valor `int`. Si bien las sentencias `switch` soportan valores `enum`, **cada** cláusula `case` debe tener el mismo tipo de datos que la variable `switch` `otherDay`, que es `int`. El resto de las líneas sí compilan. Dado que exactamente cuatro líneas no compilan, la opción E es la respuesta correcta.

**11. A.** El código compila y se ejecuta sin problemas, imprimiendo 3 en tiempo de ejecución y haciendo que la opción A sea correcta. La cláusula `default` en la línea 17 es opcional ya que todos los valores del `enum` están contabilizados (el switch es exhaustivo) y puede eliminarse sin cambiar la salida.

**12. C.** Antes de la primera iteración, `sing = 8`, `squawk = 2` y `notes = 0`. Después de la iteración del primer bucle, `sing` se actualiza a 7, `squawk` a 4, y `notes` a la suma de los nuevos valores para `sing + squawk`, `7 + 4 = 11`. Después de la iteración del segundo bucle, `sing` se actualiza a 6, `squawk` a 6, y `notes` a la suma de sí mismo más los nuevos valores para `sing + squawk`, `11 + 6 + 6 = 23`. En la tercera iteración del bucle, `sing > squawk` se evalúa como `false`, ya que `6 > 6` es falso. El bucle termina y se emite el valor más reciente de `notes` (23), por lo que la respuesta correcta es la opción C. (Nota: La explicación original dice "most recent value of sing", pero claramente se imprime `notes`).

**13. F.** El código no compila porque la cláusula `case` utiliza el operador de complemento lógico (`!`), que no está permitido con la coincidencia de patrones. Si esto se eliminara, el código aún no compilaría, ya que esta cláusula (al negar) dominaría el `case` en la línea 15 (si fuera un patrón), llevando a código inalcanzable. Específicamente, en el fragmento original de la pregunta 13 (asumiendo que era sobre `!(fish instanceof String guppy)` o similar que fue omitido en el texto de la pregunta 13 provisto antes), el error de compilación hace que F sea correcta.

**14. G.** Este ejemplo puede parecer complicado, pero el código no compila. A la línea 8 le faltan los paréntesis obligatorios alrededor de la expresión condicional booleana del `while` (debería ser `} while (keepGoing);`). Dado que el código no compila y no es debido a la línea 6, la opción G es la respuesta correcta. Si la línea 8 se corrigiera con paréntesis, entonces el bucle se ejecutaría dos veces, y la salida sería 11.

**15. B, D, F.** El código sí compila, haciendo que la opción G sea incorrecta. En el primer bucle `for-each`, el lado derecho del bucle tiene un tipo de `int[]`, por lo que cada elemento `penguin` tiene un tipo de `int`, haciendo que la opción B sea correcta. En el segundo bucle `for-each`, `ostrich` tiene un tipo de `Character[]`, por lo que `emu` tiene un tipo de datos de `Character`, haciendo que la opción D sea correcta. En el último bucle `for-each`, `parrots` tiene un tipo de datos de `List<Integer>`. Dado que el tipo genérico de `Integer` se usa en la `List`, `macaw` tendrá un tipo de datos de `Integer`, haciendo que la opción F sea correcta.

**16. F.** El código no compila, aunque no por la razón especificada en la opción E. La segunda cláusula `case` contiene una sintaxis inválida. Cada cláusula `case` debe tener la palabra clave `case`; en otras palabras, no se pueden encadenar con dos puntos (`:`) como en `case 'B': 'C':`. Por esta razón, la opción F es la respuesta correcta. Esta línea podría haberse arreglado diciendo `case 'B', 'C':` o añadiendo la palabra clave `case` antes de `'C':`; entonces el resto del código habría compilado e impreso `great good` en tiempo de ejecución.

**17. A, B, D.** Para imprimir los elementos en el arreglo `wolf` en orden inverso, el código necesita comenzar con `wolf[wolf.length-1]` y terminar con `wolf[0]`. La opción A logra esto y es la primera respuesta correcta. La opción B también es correcta y es una de las formas más comunes en que se escribe un bucle inverso. La condición de terminación suele ser `m >= 0` o `m > -1`, y ambas son correctas. Las opciones C y F causan ambas una `ArrayIndexOutOfBoundsException` en tiempo de ejecución ya que ambas leen de `wolf[wolf.length]` primero, con un índice que pasa la longitud del arreglo `wolf` basado en 0. La forma de la opción C tendría éxito si el valor se cambiara a `wolf[wolf.length-z-1]`. La opción D también es correcta, ya que la `j` es superflua y puede ser ignorada en este ejemplo. Finalmente, la opción E es incorrecta y produce un bucle infinito, ya que `w` se establece repetidamente en `r-1`, en este caso 4, en cada iteración del bucle. Dado que la sentencia de actualización no tiene efecto después de la primera iteración, la condición nunca se cumple y el bucle nunca termina.

**18. B, E.** El código compila sin problemas e imprime dos números distintos en tiempo de ejecución, por lo que las opciones G y H son incorrectas. El primer bucle se ejecuta un total de cinco veces, terminando el bucle cuando `participants` tiene un valor de 10. Por esta razón, la opción E es correcta. En el segundo bucle, `animals` comienza no siendo menor o igual a 1 (es 2), pero como es un bucle `do/while`, se ejecuta al menos una vez. De esta manera, el post-incremento hace que `animals` tome un valor de 3 y la condición se evalúa como falsa, por lo que el bucle termina, haciendo que la opción B sea correcta. Finalmente, el último bucle se ejecuta un total de dos veces, con `performers` comenzando con -1, pasando a 1 al final del primer bucle, y luego terminando con un valor de 3 después del segundo bucle, lo que rompe la condición del bucle (`3 < 2` es falso). Esto hace que la opción B sea una respuesta correcta por partida doble. Las salidas distintas impresas son 10, 3 y 3.

**19. E.** La variable `snake` se declara dentro del cuerpo de la sentencia `do/while` (entre las llaves `{ }`), por lo que está fuera de alcance (_out of scope_) en la condición de la línea 7. Por esta razón, la opción E es la respuesta correcta. Si `snake` se hubiera declarado antes de la línea 3 con un valor de 1, entonces la salida habría sido `1 2 3 4 5 -5.0`, y la opción G habría sido la respuesta correcta.

**20. A, E.** Lo más importante a notar al leer este código es que el bucle más interno (`L3`) es un bucle infinito. Por lo tanto, se buscan soluciones que salten el bucle más interno por completo o que salgan de ese bucle. La opción A es correcta, ya que `break L2` en la línea 8 hace que el segundo bucle interno salga cada vez que se entra, saltando el bucle más interno por completo. Para la opción B, el primer `continue` en la línea 8 hace que la ejecución salte el bucle más interno en la primera iteración del segundo bucle, pero no en la segunda iteración del segundo bucle (porque la condición del `if` ya no se cumple). El bucle más interno se ejecuta, y con `continue` en la línea 12, produce un bucle infinito en tiempo de ejecución, haciendo que la opción B sea incorrecta. La opción C es incorrecta porque contiene un error del compilador: la etiqueta `L3` no es visible fuera de su bucle. La opción D es incorrecta, ya que es equivalente a la opción B, dado que el `continue` sin etiqueta se aplica al bucle más cercano y, por lo tanto, produce un bucle infinito en tiempo de ejecución. Al igual que la opción A, el `continue L2` en la línea 8 permite que el bucle más interno se ejecute la segunda vez que se llama al segundo bucle. El `continue L2` en la línea 12 sale del bucle infinito, sin embargo, haciendo que el control vuelva al segundo bucle. Dado que el primer y segundo bucles terminan, el código termina, y la opción E es una respuesta correcta.

**21. D.** La línea 23 no compila porque le falta una sentencia `yield` (tiene `{ "Jane"; }` en lugar de `yield "Jane";`). La línea 24 no compila porque contiene un punto y coma extra al final (después del `}`). Finalmente, las líneas 25 y 26 no compilan porque utilizan el mismo valor `case` (`30`). Al menos una de ellas tendría que ser cambiada para que el código compile. Dado que tres líneas necesitan ser corregidas, la opción D es correcta.

**22. E.** El código compila sin problemas, haciendo que las opciones F y G sean incorrectas. Recuérdese, `var` es soportado tanto en `switch` como en bucles `while`, siempre que el compilador determine que el tipo es compatible con estas sentencias. Además, se permite la variable `one` en una cláusula `case` porque es una variable local `final`, haciéndola una constante en tiempo de compilación. El valor de `tailFeathers` es 3, lo que coincide con la segunda cláusula `case` (`case 3:`), haciendo que 5 sea la primera salida. Al no haber `break`, continúa. El bucle `while` se ejecuta dos veces, con el operador de pre-decremento (`--`) modificando el valor de `tailFeathers` de 3 a 2 y luego a 1 en el segundo bucle. Por esta razón, la salida final es `5 2 1`, haciendo que la opción E sea la respuesta correcta.

**23. F.** La línea 19 comienza con una sentencia `else`, pero no hay una sentencia `if` precedente con la que coincida. La sentencia `else` de la línea 18 ya cerró el `if` de la línea 17. Por esta razón, la línea 19 no compila, haciendo que la opción F sea la respuesta correcta. Si se eliminara la palabra clave `else` de la línea 19, entonces el fragmento de código imprimiría `Success` (debido a la asignación `older = true` en la condición del `if`).

**24. B.** Dado que esta es una sentencia `switch` de coincidencia de patrones, las ramas `case` se evalúan en el orden en que aparecen. En particular, cada rama no domina a las posteriores, por lo que el código compila sin problemas. Si se eliminara cualquiera de las cláusulas `when` de su cláusula `case` acompañante, entonces el código no compilaría (por dominación). La primera rama se salta porque "Closed" no coincide con "Open". La segunda coincide (es un Object, no es nulo y no está vacío), resultando en que se imprima 20 en tiempo de ejecución y haciendo que la opción B sea correcta.

**25. D.** El código compila sin problemas, por lo que la opción F es incorrecta. La variable `viola` creada en la línea 8 nunca se usa y puede ser ignorada. Si se hubiera utilizado como el valor `case` en la línea 15, habría causado un error de compilación ya que no está marcada como `final`. Dado que `"violin"` y `"VIOLIN"` no son una coincidencia exacta, se ejecuta la rama `default` de la sentencia `switch` en tiempo de ejecución. Al no haber un `break` en `default`, se produce un _fall-through_. Esta ruta de ejecución incrementa `p` un total de tres veces (`default: p++`, `case "VIOLIN": p++`, `case "viola": ++p; break;`), llevando el valor final de `p` a 2 (comenzó en -1) y haciendo que la opción D sea la respuesta correcta.

**26. F.** El fragmento de código no contiene errores de compilación, por lo que las opciones D y E son incorrectas. Sin embargo, hay un problema con este fragmento de código. Si bien puede parecer complicado, la clave es notar que la variable `r` se actualiza **fuera** del bucle `do/while`. Esto está permitido desde el punto de vista de la compilación, ya que está definida antes del bucle, pero significa que el bucle más interno **nunca rompe la condición de terminación** `r <= 1`. En tiempo de ejecución, esto producirá un bucle infinito la primera vez que se entre en el bucle más interno (ya que `r` se mantiene en 1), haciendo que la opción F sea la respuesta correcta.

**27. D.** El código compila y se ejecuta sin problemas, ya que cada bloque `case` contiene una sentencia `yield`. El segundo bloque `case` contiene dos rutas, las cuales terminan en una sentencia `yield`. (Si `name` es "Frog" hace `yield "Green"`; si no, hace `yield "Blue"`). En tiempo de ejecución (`amphibian` es 2 y `name` es "Salamander"), el código imprime `Blue`, haciendo que la opción D sea correcta.

**28. F.** Basado en el alcance de flujo (_flow scoping_), `guppy` está en alcance después de las líneas 41-42 si el tipo **no** es un `String` (debido a la negación `!`). En el bloque `else if` de la línea 43, se está evaluando si `fish` no es un `String`, pero en este punto, el compilador sabe que si llegamos al `else`, `fish` **sí** es un `String`, por lo que el `guppy` de la línea 41 no está en alcance, pero la línea 43 intenta declarar una variable `guppy` que es un duplicado de la variable local definida previamente en la línea 41 en el mismo ámbito más amplio. Por esta razón, el código no compila (debido a la variable duplicada `guppy`), y la opción F es correcta. Si se hubiera utilizado un nombre de variable diferente en la línea 43, entonces el código compilaría y (al pasar un `String` "goldie") la primera condición sería falsa, la segunda también sería falsa, y finalmente imprimiría `Swim!` en tiempo de ejecución.

**29. C.** Dado que se utilizó el operador de pre-incremento (`++y`), el primer valor que se mostrará es -1 (ya que `y` comienza en -2), por lo que las opciones A y B son incorrectas. En la penúltima iteración del bucle, `y` se incrementará a 5, y el bucle emitirá 5. El bucle continuará ya que `5 <= 5` es verdadero, y en la última iteración, se emitirá 6. Al final de esta última iteración, la expresión booleana `6 <= 5` se evaluará como falsa, y el bucle terminará. Dado que 6 fue el último valor emitido por el bucle, la respuesta es la opción C.

**30. E.** En la línea 43, el punto y coma debería estar **después** de la sentencia `yield` dentro del bloque, no fuera de la llave (debería ser `{ yield 4; }`). A la línea 48 le falta un punto y coma después de la sentencia `return` que contiene la expresión `switch` (debería ser `};`). Por estas razones, se deben corregir al menos dos líneas. A continuación, las líneas 43, 44 y 45 no compilan porque los valores numéricos de los `case` (int) no son compatibles con el tipo de referencia genérico `Object` (esto no está permitido en coincidencia de patrones con switch; el tipo debe ser el mismo o un subtipo). Se puede arreglar esto cambiando la línea 41 para pasar `speed` como un tipo compatible, como `Integer`. Finalmente, la cláusula `default` en la línea 46 **domina** al `case null` subsiguiente en la línea 47 (un `case null` no puede ir después de un `default`). Eliminar la línea 47 soluciona este problema, ya que el `case null` no es obligatorio y el `default` lo manejaría si no se especifica explícitamente (aunque `default` no atrapa null en expresiones switch antiguas, en Java 21, para que compile el `case null`, debe ir antes del `default`). Dado que se puede lograr que el código compile cambiando o eliminando cuatro líneas (línea 41, línea 43, línea 47 y línea 48), la opción E es la respuesta correcta.