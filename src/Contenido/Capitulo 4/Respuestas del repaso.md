**1. F.** La línea 5 no compila. Esta pregunta está verificando si se está prestando atención a los tipos. `numFish` es un `int`, y `1` es un `int`. Por lo tanto, se utiliza la suma numérica y se obtiene `5`. El problema es que **no se puede almacenar un `int` en una variable `String`**. Suponga que la línea 5 dijera `String anotherFish = numFish + 1 + "";`. En ese caso, las respuestas serían la opción A y la opción C. La variable definida en la línea 5 sería la cadena `"5"`, y ambas sentencias de salida usarían concatenación.

**2. C, E, F.** La opción C usa el nombre de la variable como si fuera un tipo, lo cual es claramente ilegal. Las opciones E y F no especifican ningún tamaño. Aunque es legal omitir el tamaño para las dimensiones posteriores de un arreglo de arreglos (multidimensional), **la primera dimensión es obligatoria**. La opción A declara un arreglo 2D legal. La opción B declara un arreglo 3D legal. La opción D declara un arreglo 2D legal. Recuerde que es normal ver clases en el examen que no se hayan aprendido (como `java.util.Date`). No se espera que se sepa nada sobre ellas.

**3. A, C, D.** La opción B lanza una excepción porque no existe el 40 de marzo. La opción E también lanza una excepción porque 2029 no es un año bisiesto y, por lo tanto, no tiene 29 de febrero. La opción F no compila porque el `enum` debería llamarse `Month`, en lugar de `MonthEnum`. La opción D es correcta porque es solo una fecha normal y no tiene nada que ver con el horario de verano. Las opciones A y C son correctas porque **Java es lo suficientemente inteligente como para ajustarse automáticamente al horario de verano**.

**4. A, C, D.** El código compila bien. La línea 3 apunta al `String` en el _string pool_. La línea 4 llama al constructor de `String` explícitamente y, por lo tanto, es un **objeto diferente** al de `s`. La línea 5 comprueba la igualdad de objetos (lógica), lo cual es verdadero (`true`), por lo que imprime `one`. La línea 6 utiliza la igualdad de referencia de objetos, lo cual no es verdadero ya que se tienen objetos diferentes. La línea 7 llama a `intern()`, que devuelve el valor del _string pool_ y, por lo tanto, es la **misma referencia** que `s`. La línea 8 también compara referencias, pero es verdadera ya que ambas referencias apuntan al objeto del _string pool_. Finalmente, la línea 9 es una trampa. La cadena `"Hello"` ya está en el _string pool_, por lo que llamar a `intern()` no cambia nada. La referencia `t` es un objeto diferente, por lo que el resultado sigue siendo `false`. Por lo tanto, las opciones A, C y D son correctas.

**5. B.** Este ejemplo utiliza el encadenamiento de métodos (_method chaining_). Después de la llamada a `append()`, `sb` contiene `"aaa"`. Ese resultado se pasa a la primera llamada `insert()`, que inserta en el índice 1. En este punto, `sb` contiene `"abbaa"`. Ese resultado se pasa al último `insert()`, que inserta en el índice 4, dando como resultado `"abbaccca"`. Por lo tanto, la opción B es la respuesta.

**6. C.** Hay que recordar observar los **tipos de retorno** en las operaciones matemáticas. Uno de los trucos está en la línea 24. El método `round()` devuelve un `int` cuando se llama con un `float`. Sin embargo, se le está llamando con un `double`, **por lo que devuelve un `long`**. El otro truco está en la línea 25. El método `random()` **devuelve un `double`** (no un `float`). Dado que dos líneas tienen un error de compilación, la opción C es la respuesta.

**7. A, E.** Al tratar con zonas horarias, lo mejor es **convertir a GMT primero restando la zona horaria**. Recuerde que restar un negativo es como sumar. La primera fecha/hora es `9:00 GMT` (5:00 - (-4:00)), y la segunda es `15:00 GMT` (9:00 - (-6:00)). Por lo tanto, la primera es más temprana por seis horas. Por lo tanto, las opciones A y E son correctas.

**8. A, B, F.** Recuerde que los índices se basan en cero, lo que significa que el índice 4 corresponde al quinto carácter (`'5'`), y la opción A es correcta. Para la opción B, el método `replace()` comienza el reemplazo en el índice 2 y termina **antes** del índice 4. Esto significa que se reemplazan dos caracteres, y se llama a `charAt(3)` sobre el valor intermedio `"1265"`. El carácter en el índice 3 es `'5'`, lo que hace que la opción B sea correcta. La opción C es similar, haciendo que el valor intermedio sea `"126"` y devuelve `'6'`. La opción D da como resultado una excepción ya que no hay un sexto carácter en el índice 5. La opción E es incorrecta. No compila porque faltan los paréntesis para el método `length()` (en `String` es un método, en arreglos es un campo). Finalmente, el reemplazo de la opción F da como resultado el valor intermedio `"145"`. El carácter en el índice 2 es `'5'`, por lo que la opción F es correcta.

**9. A, C, F.** Los arreglos están indexados desde cero, lo que hace que la opción A sea correcta y la B incorrecta. **No son capaces de cambiar de tamaño** (_fixed size_), lo cual es la opción C. Los valores internos sí pueden ser cambiados, lo que hace que la opción D sea incorrecta. Un arreglo **no sobrescribe** `equals()`, por lo que utiliza la igualdad de referencia de objetos de `Object`. Dado que dos objetos diferentes no son iguales por referencia, la opción F es correcta, y las opciones E y G son incorrectas.

**10. A.** Todas estas líneas compilan. Los métodos `min()` y `floor()` devuelven el mismo tipo que se pasa: `int` y `double`, respectivamente. El método `round()` devuelve un `long` cuando se llama con un `double`. La opción A es correcta ya que el código compila.

**11. E.** Un `LocalDate` **no tiene un elemento de tiempo**. Por lo tanto, no existe el método para añadir horas (`plusHours()`), lo que hace que la opción E sea la respuesta.

**12. A, D, E.** Primero, hay que notar que la llamada a `indent()` añade un espacio en blanco al principio de `numbers`, y `stripLeading()` lo elimina inmediatamente. El método `substring()` tiene dos formas. La primera toma el índice para comenzar y el índice para detenerse inmediatamente **antes**. La segunda toma solo el índice para comenzar y va hasta el final del `String`. Recuerde que los índices se basan en cero. La primera llamada comienza en el índice 1 y termina con el índice 2 ya que necesita detenerse antes del índice 3 (`"12"`). Esto da la opción A. La segunda llamada comienza en el índice 7 y termina en el mismo lugar, resultando en un `String` vacío, que es la opción E. Esto imprime una línea en blanco. La llamada final comienza en el índice 7 y va hasta el final del `String` (`"78"`), terminando con la opción D.

**13. B.** Un `String` es inmutable. Llamar a `concat()` devuelve un nuevo `String` pero **no cambia el original** (y el resultado se ignoró en el código). Un `StringBuilder` es mutable. Llamar a `append()` añade caracteres a la secuencia de caracteres existente y también devuelve una referencia al mismo objeto. Por lo tanto, `roar1` sigue siendo `"roar"` y `roar2` se convierte en `"roar!!!"`. La opción B es correcta.

**14. B, F.** Las opciones A y C son incorrectas porque no hay un método `asTime()` o `withTime()` definido en `LocalDate`. La opción B crea correctamente un `LocalDateTime` a partir de un `LocalDate` y `LocalTime` (`date.atTime(time)`). La opción E es incorrecta porque `Instant`, al igual que otras clases de fecha/hora, no tiene un constructor público y se instancia mediante métodos estáticos o de conversión. La opción F es la conversión adecuada (`zonedDateTime.toInstant()`). La opción D es incorrecta porque el objeto de origen (`LocalDateTime`) no representa un punto universal en el tiempo. Sin una zona horaria, Java no sabe qué momento en el tiempo utilizar para el `Instant`.

**15. C, E.** Los números se ordenan antes que las letras, y las mayúsculas se ordenan antes que las minúsculas (`[123, PIG, pig]`). Esto hace que la opción C sea una de las respuestas. El método `binarySearch()` busca dónde se insertaría el valor (`"Pippa"`). Iría antes de `"pig"`, que es el segundo elemento (índice 2, ya que `"PIG"` está en el 1 y `"Pippa"` iría entre `"PIG"` y `"pig"`). Luego lo niega (`-2`) y le resta uno (`-2 - 1 = -3`), lo cual es la opción E.

**16. A, G.** El método `substring()` incluye el índice inicial pero **no el índice final**. Cuando se llama con 1 y 2, devuelve un `String` de un solo carácter (el del índice 1), lo que hace que la opción A sea correcta y la E incorrecta. Llamar a `substring()` con 2 como ambos parámetros es legal. Devuelve un `String` vacío, lo que hace que las opciones B y F sean incorrectas. Java no permite que los índices se especifiquen en orden inverso (inicio mayor que fin). La opción G es correcta porque `substring(6, 5)` lanza una `StringIndexOutOfBoundsException`. Finalmente, la opción H es incorrecta porque devuelve un `String` vacío (`substring(6, 6)` es válido).

**17. C, F.** Esta pregunta es engañosa porque tiene varias partes. Primero, hay que saber que el bloque de texto en las líneas 13 y 14 es equivalente a un `String` regular. Dado que no hay un salto de línea al final (porque el cierre `"""` está en la misma línea que el texto), se trata de cuatro caracteres (`"purr"`). Luego, se debe saber que los objetos `String` son inmutables, lo que significa que **los resultados de las líneas 17-19 son ignorados** (porque no se reasignan). Finalmente, en la línea 20 ocurre algo. Se concatenan tres nuevos caracteres a `s1` (`"purr" + "two"`) y ahora se tiene un `String` de longitud 7, lo que hace que la opción C sea correcta. A continuación, `s2 += 2` se expande a `s2 = s2 + 2`. Un `String` concatenado con cualquier otro tipo da un `String`. Las líneas 22, 23 y 24 agregan a `s2`, dando un resultado de `"2cfalse"`. La sentencia `if` de la línea 27 devuelve `true` porque los valores lógicos de los dos objetos `String` son iguales utilizando la igualdad de objetos (`equals()`). Por esta razón, la opción F es correcta. La sentencia `if` de la línea 26 devuelve `false` porque los dos objetos `String` no son los mismos en memoria (no tienen la misma referencia `==`). Uno proviene directamente del _string pool_ (el literal en la comparación), y el otro proviene de la construcción usando operaciones `String` (`+=`).

**18. A, B, D.** El método `compare()` devuelve un número entero **positivo** cuando los arreglos son diferentes y **el primero es mayor**. Este es el caso de la opción A ya que el elemento de `s2` en el índice 1 (`"Llama"`) viene primero alfabéticamente que el de `s1` (`"Peacock"`), haciendo que `"Peacock"` (el primero) sea considerado mayor. No es el caso de la opción C porque `s4` es más largo (así que el segundo es mayor) o de la opción E porque los arreglos son iguales. El método `mismatch()` devuelve un número entero **positivo** (el índice) cuando los arreglos son diferentes en una posición de índice 1 o mayor. Este es el caso de las opciones B y D ya que la diferencia está en el índice 1. No es el caso de la opción F porque no hay diferencia (devuelve `-1`).

**19. A, D.** El objeto `dateTime1` tiene una hora de 1:30 según la inicialización. El objeto `dateTime2` es una hora después. Sin embargo, no existe la hora 2:30 cuando se adelanta el reloj, por lo que **se establece la hora en las 3:30**. La opción A es correcta porque ha pasado exactamente 1 hora real (`diff = 1`). La opción D también es correcta porque la hora local del nuevo tiempo es 3 (`hour = 3`). La opción E no es correcta porque se ha cambiado el desplazamiento de la zona horaria debido al horario de verano (`offset = false`).

**20. A, C.** El método `reverse()` es la forma más fácil de invertir los caracteres en un `StringBuilder`; por lo tanto, la opción A es correcta. En la opción B, `substring()` devuelve un `String`, que no se almacena en ninguna parte, y el original quedaría como `"JavavaJ$"`. La opción C utiliza encadenamiento de métodos. Primero, crea el valor `"JavavaJ$"`. Luego, elimina los primeros tres caracteres (índices 0, 1, 2), lo que da como resultado `"avaJ$"`. Finalmente, elimina el último carácter (el de la longitud original - 1), resultando en `"avaJ"`. La opción D lanza una excepción porque no se puede eliminar el carácter posterior al último índice (que sería `.length()`). Recuerde que `deleteCharAt()` utiliza índices basados en cero, y `length()` cuenta el número total de caracteres en lugar del índice.

**21. A.** La fecha comienza como el 30 de abril de 2025. Dado que las fechas son **inmutables** y se ignoran los valores de retorno de los métodos `plus...()`, el resultado (la variable original `date`) permanece sin cambios. Por lo tanto, la opción A es correcta.

**22. E.** El código crea primero una fecha de `2025-10-31`. El encadenamiento en la salida es válido y da lugar a estados intermedios de `2026-10-31` (después de `plusYears(1)`), seguido de `2026-05-31` (después de `plusMonths(-5)`), luego `2026-06-30` (después de `plusMonths(1)`, como junio no tiene 31 días, se ajusta al 30), luego de nuevo `2026-06-30` (después de `withYear(2026)` no hay cambios), y finalmente se combina con la hora dando el resultado de `2026-06-30T13:04`.